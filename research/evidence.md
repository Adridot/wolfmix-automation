# Evidence registry

Every finding this repository has published, one line each: the date, what was
done, what came out, and the status it ended at. `SPEC.md` is the consolidated
read of these; this is the ledger it was built from, and when the two disagree
this file is the one that was written next to the measurement.

**Reading a line.** The date is the day of the *measurement*, not of the
write-up. Status uses the vocabulary in [`AGENTS.md`](../AGENTS.md) —
`observed → hypothesized → correlated → validated → device-confirmed` — and an
arrow marks a status that **moved**, in either direction. Unless a line says
otherwise the controller ran firmware **2.0.18** and the editor was WTOOLS
1.6.3 (until 2026-08-30) or 2.0.2 beta (after); a line that depended on a
version says so. `snapshot` names a directory under `corpus/experiments/`: the
project files themselves are not in this repository, their SHA-256 are, in
[`corpus/SHA256SUMS`](../corpus/SHA256SUMS).

**Refutations come first, and in full.** They are the entries that cost the
most and the ones a summary is most tempted to lose. A status that went **down**
is written out below with what it dragged with it; the table then carries every
entry, refuted ones included.

## Refutations, withdrawals and status downgrades

### RECALL-01 — `device-confirmed` → **withdrawn** (2026-08-27)

Read: "the varint value of `SET_PRESET` is ignored — the event addresses
nothing." Measured from two DMX envelopes captured separately.

**Why it was wrong.** Two separately captured envelopes cannot separate
"nothing moved" from "the same thing was repainted". Preset recalls do act, and
they always act the same way, which the separate captures rendered
indistinguishable from inaction. One continuous capture with timestamped
transitions settled in minutes what they had got backwards.

**What it dragged down.** Four negatives rested on the same discriminator and
went with it; the repair is in RELOAD-02, and RECALL-03 is the reading that
replaced it — the payload is not protobuf at all, and the device had been
reading our tag byte as the index (RAW-01).

### GEN-02 — `device-confirmed` → `hypothesized` → `validated` (2026-08-27)

Read: "`165.f10` bit 5 (`OTHER`) is what silences the dimmers."

**Why it went down.** The dimmer silence it was thought to explain turned out
to be a **controller setting** that had been off the whole time. One cause is
enough, and the field had never been isolated. It climbed back to `validated`
the same week, but only because FW-03 wrote that bit **and nothing else**: a
status goes back up by measurement, not by the passage of time.

### F4-01 — `device-confirmed` → **retracted** (2026-08-26), superseded by F4-02

Read: "`COLOR` = `f4` bit 3, `MOVE` = bit 2, `BEAM` = bit 4", from six
photographed `PRESET EDIT` screens.

**Why it was wrong.** Wrong field. The content mask is **`f10`**. `f4` and
`f10` are *both* constant per factory page, so six screens could not tell them
apart — the four `f4` values happened to be in one-to-one correspondence with
the four `f10` values across the four pages. It took a write that moved one
without the other.

This is the corpus-uniformity trap in a new dress: not one field uniform
everywhere, but **two fields co-varying everywhere**, which is just as
indistinguishable and just as easy to mistake for a proof.

**What survives.** The mask *shape* — `Searchlight` lighting three toggles at
once still refutes an enumeration — and the retraction of ACC-03's "closed
vocabulary" reading. `f4` itself became unknown again, and F4-03 later read it
as a **permission mask** over the three engines.

### FX2-01 — an identity refuted the day it was added (2026-08-27)

`wpj_identities.py` gained "an FX engine that is on stores two *different* page
configurations" that morning: 3697 occurrences, zero exceptions. One preset
composed at the panel refuted it the same evening.

A correlation with no exception over the whole corpus is a fact **about the
corpus** until a fresh write tests it. The defence is the write, not more
counting — and the author of that warning fell into it inside a day.

### FLASH-01 — the published prediction was refuted, and took an identity with it

Predicted: the latched flash state lands in `165.f12` or `f20`, the two holes in
the field numbering. Measured: neither appeared; **`f16` slice 6** moved
instead, to 511 — all **nine** slots, the eight groups plus the one holding the
fogger and the spark.

It also refuted one of our own identities: `moteurs_f16` asserted that no slice
ever sets the ninth bit — true on 2778 presets, false the moment a flash key is
latched. **It lasted four hours.** The identity now checks that for slices 0–5
only, and the comment says why.

### F11-03 — refuted, and the experiment could not have tested what it claimed

`Button brightness` set to 14 %. Predicted `102.f11` = 14. Measured: one save,
43688 bytes both times, **not one record changed** — the setting is not project
data, which is a clean result.

**But the experiment was mis-posed, and the fault is the author's.** The
prediction rested on "`f11` = 100 in every corpus file". That had stopped being
true twenty-eight snapshots earlier: F11-01 wrote `f11 = 0` into that very
project, and every save since re-emits the field absent. The comparison was 0
against 0.

> Read the current state of the field you are about to test, not the state the
> registry describes.

### COV-22 — the `125.f4` / record 161 coupling, **refuted** (2026-09-01)

Read: "the 7th byte of `125.f4` moves **in the same save** as the shared tail of
record 161, in both projects where more than one version exists."

**Why it was wrong.** A save on 2026-09-01 moved the tail — all three items,
index 187, `176 → 240`, together and to the same number, exactly COV-21's
reading — while record **125 came back byte-for-byte identical**, its 7th byte
`0x38` on all nine slots before and after. Two saves in two projects were the
whole basis; the third save separates them.

What survives is COV-22's actual result, which was itself a refutation: the
byte does **not** track preset creation, and it moves back, so it is no one-way
flag. It is unattributed again, and so is 161's tail. Nothing was named on the
coupling, which is why nothing else falls with it.

### F11-01's side finding — "loading a project resets the flash release modes" — **retracted**

`102.f4` came back as `01 00 00 00 00 00` because **the operator reset those
modes by hand**, not because loading a project clears them. The SMOKE reversion
noted in FX-07 remains a single unexplained observation rather than the general
behaviour that entry claimed.

### EXP-06 — a prediction refuted in the middle of an otherwise exact result

Predicted, as the discriminator for the DMX address base: `f12.f3 = 115.f2 − 1`
(base 0 against base 1). Measured: **exact equality — both are base 0.**

The rest of the Rosetta pair held: profiles field for field, preset names, and
the FX tuples — though not as predicted either. The four 81-byte tables are
**banks per effect** (row *e* = the parameters of effect *e*), not tuples per
group; 21/21 engine×preset. The effect id is the **row index**, and it stayed
invisible because it was being looked for as a value.

### F30-02 — element 7 refuted the share rule that six derived rows had assumed

Predicted for `inward hard`: Amber 1,2,9,10 · Cyan 3–8. Measured: **Amber
1,2,3,8,9,10 · Cyan 4–7** — direction right, shares wrong. Seven of the eight
captures were exact; this one corrected the distribution rule, which F30-03 then
closed at three colours in both geometries.

`FLASH` also turned out to make a group's pads **momentary**, not a spatial
layout.

### F7-03 and F7-04 — two predictions the author got wrong, kept as written

- **F7-03**: predicted `f16` slice 1 (Move) = 0, reading "engine stopped" off a
  photograph of the `MOVE FX` tiles. Measured **4 = C**: the engine was on. The
  screen reading was wrong, or the operator lit it while setting the page, and
  nothing in the photographs can tell which.
- **F7-04**: the same box was left **empty** on purpose after that miss.
  Measured 3 = A+B. The abstention cost the shot nothing.

Both entries also carry a method note against themselves: the probabilities were
published **before reading** the file, but the operator had already measured. The
protection against retrospective confirmation holds — nothing had been read —
but "published before the measurement" does not apply, and the commit date alone
would have suggested otherwise.

### SCREEN-02 — one of six measures refuted its prediction (2026-08-31)

Predicted that `mode presets` would move the panel to the preset screen. It
reports `wolfmixMode: 5` and the panel **stays on HOME**. Recorded as false
rather than rewritten; the measured partition is in the table.

### GUARD-01 — one of nine measures refuted its prediction (2026-08-31)

The pre-deploy archive failure surfaced a bare `PermissionError` instead of the
named refusal the prediction claimed. Fixed in the same session — the message
now reads "Pre-deploy archive failed, nothing was uploaded: …" — and re-measured.

### COV-02 — `165.f27` is not zero everywhere (2026-09-01)

SPEC §5.6 and Q1 both stand on "`f27` is zero on all 3697 corpus presets".
On the corpus as it is today — 4312 presets over 76 variant-A files — **26 of
them carry `[1000] × 8`**, and the statement was true when it was written: the
files that carry it were added on 2026-08-31, after the sentence.

The split is by author, not by content. Every preset of the three projects a
**WTOOLS 2.0.2 duplicate** produced (`WMX EXP05`, `2 Lyres-v2`) carries 1000 on
all eight groups; every preset of the projects composed **at the panel**
(`WMX EXP format-lab`, the four rigs) carries eight explicit zeros. Neither
side omits the field.

One preset crosses the line inside the corpus. In EXP-07 the device recaptured
`WMX EXP05`'s preset 0, and its `f27` came back `[0] × 8` while the six
untouched presets kept 1000. That is not a single-variable differential —
the recapture rewrote seventeen fields of that preset at once, `gobo_focus`
`50 → 0`, `gobo_zoom` and `gobo_iris` `100 → 0` among them — so it names the
author and not the meaning.

What it does buy is the **cheap discriminator Q1 says has not emerged**: the
contrast is reachable in WTOOLS, on a project already on this machine, with no
preset capture and no deploy. 1000 is the shape of a millisecond default, next
to `f11` FADE and `f15` HOLD which are milliseconds — and it sits in the same
group of per-group tables whose WTOOLS defaults this same diff shows written
out explicitly (`50 / 0 / 100 / 100` on `f32`–`f35`, §5.6). **Hypothesis, not a
reading**: a sixth per-group table whose WTOOLS default is 1000 and which this
firmware writes as 0.

Q1 is not answered. Its stated blocker is.

### COV-03 — record 120's entries are not empty (2026-09-01)

The unnamed-field inventory read `120.channels` as "`2a 00` entries, all
empty", which would have made it structurally empty on this rig rather than
undecoded. Measured: **10 754 of 14 698 entries carry content** — 73.2 %. The
fields are `f1` (9680 occurrences, DMX-shaped values), `f4` (7876, always 1),
`f5` (2148, only 1 or 2) and `f6` (2081).

The one file where the entries really are empty is *rig-c-bug*: 251 of 258, which
is **BUG-01**, the firmware erasure — not a property of the record. Reading the
corpus-wide shape off that file is the mistake the erasure invites.

No identity fell out. `f6` comes in pairs `(n, n−1)` whose `f5` reads 1 then 2,
which looks like a coarse/fine couple, and `f6` matches `110.principal_channel`
on 954 of the 2081 entries that carry it — enough to notice, nowhere near
enough to name. It stays unread, with a shape.

### COV-14 — `f16` slice 1 is not equal to `move_fx_active` (2026-09-01)

`slice 1 == move_fx_active` was `correlated 45/45`, held on 3697 presets, and
was one of the identities `wpj_identities.py` asserts. **The preset the device
composed for COV-10 carries slice 1 = 1 with `move_fx_active` = `[0]`.** The
counter-example is a preset the firmware wrote alone, on a capture whose only
deliberate variable was a per-group position FADE.

The identity was not wrong when it was written. It was **untested**: the two
files that break it — a WTOOLS-duplicated project and the capture made into it
— entered the corpus in the same commit that broke it. Adding two files to a
corpus of 78 refuted a claim that had stood on 3697 presets, which is the
clearest statement of the corpus rule this repository has: uniformity across a
corpus is a fact about the corpus.

What survives is the containment, exact on every preset:

    slice 1 ⊇ move_fx_active

and it keeps the whole refuting power — at a stride of 8 it fails on 1767 of
4339 presets, so the alignment still tests itself.

**Slice 0 against `color_fx_active` is now asserted at neither.** Both
containments fail — 6 presets one way, 18 the other — so `wpj_identities.py`
checks nothing there rather than checking a fitted rule. SPEC's "equals
`color_fx_active`, 3696/3697" is a count on a corpus that no longer exists.

Two readings of what slice 1 means are open and this measurement does not
separate them: that it marks the groups whose **movement** the preset touches,
a fade being movement; or that a capture sets it from live state that
`move_fx_active` does not track. Nothing here chooses.

### The unnumbered ones, for completeness

- **ACC-03's "closed vocabulary"** reading of `f4`: retracted by F4-01, which is
  itself retracted by F4-02. The display bug ACC-03 hit still has no cause.
- **`f30` read as a scalar** replicated eight times: refuted by one photograph
  of the device, then by F30-04's `[4, 3, 8, 0…]` — the first non-uniform `f30`
  ever observed, after 2033 corpus presets carrying the same value on all eight
  groups.
- **`165.f16` slice 5 "always 255"**: `correlated` over 2446 presets, refuted
  when the device's own writer put the mask of the groups each preset addresses
  there (GEN-03).
- **`f2` of the FX sub-message read as "speed %"** since ACC-04: it is the
  **fade**. The speed is `f9`, and a whole campaign was framed as a three-way
  permutation that was really a four-way, because `f2` already carried a name
  and was never put back on the table (FX6-02).
- **Record 155 "the 4 FX sequences"**, `device-confirmed`: the beam sequencer
  was hunted in an opaque blob (161) while the answer sat in 155 behind `f2`, a
  field named "sequence flavour" and never opened (FX6-05).

## Three ids mean two things each

Found while building this file, and worth more than the entries around it: three
ids were reused for **different experiments**. A citation that gives only the id
is ambiguous for these three, and every citation in the tree has been checked
against the intended one.

| id | first use | second use |
|---|---|---|
| `SCREEN-02` | 2026-08-27 — how to leave a modal screen | 2026-08-31 — which modes actually move the panel |
| `EXP-07` | 2026-08-25 (planned) — change a Beam FX type | 2026-08-31 — single-variable differentials on a variant-B project |
| `GOBO-01` | 2026-08-26 — the five static gobo features (**refuted**) | 2026-08-30 — where the gobo icon library lives |

They are **not renumbered**: a published id is a citation target, and rewriting
one to tidy the ledger is the same move as rewriting a refuted prediction. The
ambiguity is recorded instead, which is what this repository does with
ambiguity everywhere else.

## The register

`SPEC §` is filled where the source states it; a dash means the link has not
been made, not that there is none.

### Acceptance of files we wrote — the write chain

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| EXP-01, EXP-02, EXP-03, EXP-04 | 2026-08-25 | **planned only**: rename a project, rename a preset, one FX parameter, a null round trip. Superseded by ACC-01…04 and by the corpus round trip. | planned | — |
| ACC-01 | 2026-08-25 | A copy of `rig-a` with record 101 rewritten, imported into WTOOLS 1.6.3 and synced to the W1 → accepted, **stored byte for byte identical**, opened on the device. The variant-A container, the SHA-1 header and the 101 encoding all land at once. | device-confirmed | §2 |
| ACC-02 | 2026-08-25 | The same chain with a Color FX and dimmers added → the preset **name** applied, the FX and dimmers **ignored**. First sign that a mask gates what the W1 reads. | device-confirmed (name) | §5 |
| ACC-03 | 2026-08-25 | Two masks tried (`f4` = 25, then 255 + flag) → 25 produced a **UI bug** on the device, 255 was accepted and still ignored. Read as "`f4` is a closed vocabulary" — retracted by F4-01, whose own reading was then retracted by F4-02. The display bug still has no cause. | refuted | — |
| ACC-04 | 2026-08-25 | In-place edit of factory preset `Carnival`, `f7` 3 → 2 → the device shows a colour chaser. The preset-edit chain works end to end. Its by-product — reading `f2` as "speed %" — cost a whole campaign four days later (FX6-02). | device-confirmed | §5 |
| ACC-05 | 2026-08-25 | Planned discriminator for `f5` (the 16-bit pad mask) and the pads↔UI relation. Closed from the corpus instead, without generating a file. | planned, closed on corpus | §5 |
| ACC-06 | 2026-08-25 | Built (`corpus/experiments/ACC-06/build.py`): separate speed `f2` from `f9`, and read the second varint of the pad mask. The speed half was **answered the other way round** four days later — `f9` is the speed (FX6-02). | superseded | §5 |
| EXP-05 | 2026-08-31 | Duplicate the variant-**C** project under WTOOLS 2.0.2 → **refuses to open**: "the project comes from too old a version". The 6→8 migration path is closed on this machine. Rival 3 of 3 won. | observed | §1 |
| EXP-05bis | 2026-08-31 | Fallback, duplicate a **v7** project → WTOOLS 2.0.2 offers no duplication at all; done on the W1, and the store failed with `invalid wire_type` on both sides. Nothing was written. | observed | §1 |
| EXP-06 | 2026-08-31 | The **Rosetta pair**: the same show in both serialisations. Profiles, patch and preset names align field for field; the base-0/base-1 discriminator is **refuted** (both base 0); the FX tables are **banks per effect**, and the effect id is the row index. | device-confirmed | §1, §5 |
| EXP-07 | 2026-08-31 | Three single-variable edits on a variant-B duplicate. (c) gobo rename → `145[A][0].f3`, the RENAME-01 field, written by the device this time. (b) Color FX speed 50 → 83 → `f9`, but the save **recaptures** the whole preset. (a) DMX address 17 → 18 → exactly three records: `115.f2`, the eight `106` entries of that fixture, and `120`'s derived header. | device-confirmed / validated | §7 |
| EXP-08, EXP-09 | 2026-08-25 | **Planned only**: isolate `f28` (position) and `f31` (static colour) with single-pad edits. Both fields were later measured by other routes (POS-07, GEN-01/03). | planned, superseded | §5.1 |

### Record 102 — the flash FX settings

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| FX-01 | 2026-08-25 | `STROBE` moved FLASH → TOGGLE and saved → **exactly one byte** of `102.f4` changed, at index 1. The operator's independent report that `WOLF` was already TOGGLE matched index 0. | device-confirmed | §6 |
| FX-02 | 2026-08-25 | Five release modes set at once, prediction `01 00 01 02 03 04` published first → **exact match, five bytes**. The whole enumeration (FLASH / TOGGLE / 1 s / 5 s / 10 s) is settled. | device-confirmed | §6 |
| FX-03 | 2026-08-25 | A save in which the operator opened the MOVE FX sequence editor → record **155** moved for the only time in five controller saves. Recorded as save noise, and it later mattered: 155 is the sequencer (FX6-05). | observed | §6 |
| FX-04 | 2026-08-25 | Four consecutive saves (strobe 100 %, speed 8×, group A excluded, speed 2×) → `f9` = STROBE speed 1–100, `f7` = SPEED multiplier, `f8` = excluded-group bitmask. Record 102 was the only record that changed. | device-confirmed / correlated | §6 |
| FX-05 | 2026-08-25 | SPEED 2× → 4× → `f7` = 4, which rules out a plain list index (it would have written 3). | correlated | §6 |
| FX-06 | 2026-08-25 | SPEED → 0.5× → `f7` = **1**. A literal multiplier cannot store 0.5, so `f7` is an enumeration that coincides with the multiplier on the integer steps and reserves 1 for the half. | device-confirmed | §6 |
| FX-07 | 2026-08-25 | Seven settings in one save → `f5` = SMOKE fan, `f6` = SMOKE intensity, both by unique value. One pair could not be separated by this save, and FX-08 separated it. SMOKE's release mode reverted on its own, unexplained and never reproduced. | device-confirmed | §6 |
| FX-08 | 2026-08-25 | BLINDER fade-out 2 s → 1 s and SPEED FREEZE → 2× → the FX-07 collision resolves: `f2` = BLINDER fade-out. Record 102 readable end to end. | device-confirmed | §6 |
| FX-09 | 2026-08-25 | BLACKOUT excludes D, STROBE excludes A **and** B → `f8` 1 → 3: the exclusion field is a genuine bitmask. Master brightness 80 % **wrote nothing** — the first setting shown not to be project data. | device-confirmed | §6 |
| FX-10 | 2026-08-25 | WOLF excludes group F → `f10` = 32. Record 102 is closed except `f11`, which F11-01 then measured as inert. | device-confirmed | §6 |
| F11-01 | 2026-08-26 | `102.f11` written ourselves, 100 → 0, uploaded and compared → **no visible change, and 2048 DMX channels identical** over two 12-second envelopes; then two more with the **blinder active**, 0 channels differing while the blinder itself plainly moves 48. The BLINDER hypothesis is refuted; the field is unattributed and inert. | correlated | §6 |
| F11-03 | 2026-08-26 | `Button brightness` 100 % → 14 % → **not one record changed**. The setting is controller-global. The prediction it was meant to test could not have been tested — see the refutations above. | observed — its own prediction refuted | §6 |

### Record 165 — the preset, and its masks

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| F4-01 | 2026-08-26 | Six `PRESET EDIT` screens photographed, no write → `f4` is a **bitmask**, not an enumeration (`Searchlight` lights three toggles at once). The bit assignments it published are **retracted** by F4-02. | shape device-confirmed, bits retracted | §5.3 |
| F4-02 | 2026-08-26 | Two toggles switched off on two presets, one save → **`f10`** moved, `f4` did not. `f10` is the six toggles in screen reading order, a set bit meaning **OFF**; six photographs re-read without a contradiction. Then confirmed in reverse, byte for byte: toggling off, on and off again returns the project to identical bytes. | device-confirmed | §5.3 |
| F4-03 | 2026-08-26 | Corpus cross of `f4` against the `f16` engine masks → `f4` is a **permission mask** over the three engines, one-directional; slice 2 of `f16` is the Beam FX; slice 7 is the strobe (two presets in 2446); slice 5 is the static layer, hypothesized. | correlated | §5.3, §5.4 |
| F7-01 | 2026-08-26 / 08-27 | First shot, a negative: recalling `Get Moving` moves 110 channels, so `f7` cannot be isolated from an envelope. Second shot on the panel: `f7` is the **Color FX page selector** per group — and both the earlier correlation and its attribution were refuted on the way. | device-confirmed | §5.6 |
| F7-02 | 2026-08-27 | Group C set to `MOVE FX2` and saved → **nothing moved in record 165**. The rival at p = 0.25 won: a project save does not write the live page; only a `SHIFT` + pad capture does. The version counter incremented while **all 45054 payload bytes stayed identical** — an increment proves nothing about content. | validated → device-confirmed | §5.6 |
| F7-03 | 2026-08-27 | The same state **captured** into a preset → `f23` = `[0,0,1,0,…]`, exactly as predicted, and `f7` rigorously unchanged. `165.f23` is the Move FX page selector; the record lays its engines out in regular quadruplets. The prediction about `f16` slice 1 was **wrong** (see refutations). | validated | §5.6 |
| F7-04 | 2026-08-27 | Group A to `BEAM FX2`, captured → `f3` = `[1,0,0,…]`, and the two earlier selectors did not move by a byte. The quadruplet holds on all three engines. The operator's unplanned control — setting B to `BEAM FX1` explicitly — showed that a hand-set page 1 is **indistinguishable** from a page never set. | validated | §5.6 |
| F11-02 | 2026-08-26 | Four `f11` values against six photographed screens → `f11` is **FADE in milliseconds**, every value as predicted. Negative in the same shot: the screen's `COLOR` line does not read `f31`, and where it reads from is open. | device-confirmed | §5.3 |
| FLASH-01 | 2026-08-26 | `WOLF` latched, preset overwritten, one save → the prediction (`f12`/`f20`) is **refuted**; `f16` slice 6 rises to **511** — all nine slots. Took one of our own identities with it. | device-confirmed | §5.7 |
| FLASH-02 | 2026-08-26 | `BLINDER` latched → slice **9**, and slice 6 falls back to 0 in the same capture. The positional inference from FLASH-01 is **retracted**: two points were consistent with the order and with other arrangements. | device-confirmed | §5.7 |
| FLASH-03 | 2026-08-26 | `BLACKOUT` latched → slice **8**, while slice 9 stays up because the key was still down. Two flash engines at once: the slices are independent, not a one-of-N selector. Three points, and still no order. | device-confirmed | §5.7 |
| FLASH-04 | 2026-08-26 | `SMOKE` latched → **no slice rose**. Closed by FLASH-05's capture, where `SMOKE` was demonstrably held alongside `STROBE` and wrote nothing. | correlated → device-confirmed | §5.7 |
| FLASH-05 | 2026-08-26 | `STROBE` latched → slice **7**, value **511**, not the 255 that `Fire!` and `Strobe` carry: the flash key and a preset's own strobe engine are different authors. | device-confirmed | §5.7 |
| FLASH-06 | 2026-08-26 | `SPEED` latched → slice **10**. **Retracts** "SPEED has no slice, by symmetry", and with it the rule that "has an exclusion mask in 102" ⇔ "has a slice in `f16`" — exact on the five measured at the time, false on the sixth. | device-confirmed | §5.7 |
| FLASH-07 | 2026-08-26 | `SMOKE` alone latched, everything else released → nothing rose. The cleanest form the experiment can take: one key active, the only candidate left, not a bit written. Slice 11 did not move either. | device-confirmed | §5.7 |
| FLASH-08 | 2026-08-26 | The reverse direction: three presets written with one flash slice each, deployed, recalled → all three fired their predicted key. **But only after a manual reopen** — a deployed project is stored, not live, and `RESTART` does not substitute. | device-confirmed | §5.7, §10.1 |
| FLASH-09 | 2026-08-26 | Two preset entries duplicated into a file, deployed → the controller returned 83, not 85. Read as "the device deletes entries it did not create". **Retracted** by PRESET-01: the pre-flight download showed the state had been misattributed. The drop happens at reopen, not at store. | retracted; the snapshot pair survives as a check fixture | §10.1 |
| FLASH-10 | 2026-08-26 | Slices 9 and 10 written into a file and recalled → both fire. The five flash slices are confirmed in **both directions**. Presets 83 and 84 exist, which is what re-opened the FLASH-09 question. | device-confirmed | §5.7 |
| PRESET-01 | 2026-08-26 | Pre-flight download before the planned experiment → 85 presets, ids up to 84: the previous day's headline **collapses before the experiment runs**. All three predictions then held on the deploy, and `f16` slices 9/10 became device-confirmed in the write direction. | device-confirmed | §10.1 |
| PRESET-02 | 2026-08-26 | Planned discriminating deploy with `f1` bumped to 83. Overtaken by PRESET-01's result. | planned, superseded | §10.1 |
| PRESET-03 | 2026-08-26 | A named preset written at page 5 slot 20 (id 99), hands off → the pad does not appear before a manual reopen, and on reopen the panel **rejects the project outright** — the first rejection ever measured. Refuted in an unforeseen direction. | observed | §10.1 |
| PRESET-04 | 2026-08-26 | The same, with the id gap filled → **dense ids still refuse to load**. The gap was not the variable. | observed | §10.1 |
| PRESET-05 | 2026-08-26 | One entry, a 23-byte name → the loader refuses. The **19-byte name limit** is the loader's, and exceeding it bricks the project open. | device-confirmed | §5.8 |
| PRESET-06 | 2026-08-26 | The same with a 16-byte name → it loads. The A/B pins the failure on name length and clears the rest. | device-confirmed | §10.1 |
| PRESET-07 | 2026-08-26 | A sparse layout, matching what the device's own save writes → loads. `SET_PRESET.f2` re-read as the entry **position**, 0-based and clamped — itself later refuted by RECALL-01/RAW-01. | device-confirmed on the file side; the `f2` reading refuted | §10.1, §10.2 |

### Static layers — colour, gobo, position

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| F29-01 | 2026-08-26 | Three read-only DMX envelopes, nothing written → the static gobo index is **0-based**, 255 = none. All three matched the published 0-based prediction; the 1-based alternative predicted different values. Closing envelope 2048/2048 against baseline. | device-confirmed | §5.1 |
| F30-01 | 2026-08-26 | Four read-only captures → five stored `PATTERN` values read off factory presets, and the eleven-entry icon list read off the encoder. **Retracts** "one scalar replicated eight times" — the screen shows three groups on different values at once. | device-confirmed | §5.1 |
| F30-02 | 2026-08-26 | The whole list walked live on group B, eight captures, each predicted in writing first, **nothing written** → seven exact; element 7 corrected the share rule. `FLASH` makes a group's pads **momentary**, not a spatial layout. | device-confirmed | §5.1 |
| F30-03 | 2026-08-26 | Two more captures at three colours in both geometries → the distribution rule, complete: deal in ascending pad order over the run, split as evenly as possible, remainder to the earliest colours. Enough to generate a static look for any rig. | device-confirmed | §5.1 |
| F30-04 | 2026-08-26 | One save, three groups on three patterns with three colour selections → `f30 = [4, 3, 8, 0…]` and three different masks, **every value exact**. The first non-uniform `f30` ever observed, after 2033 corpus presets carrying one value on all eight groups. | device-confirmed | §5.1 |
| GOBO-01 | 2026-08-26 | Five `STATIC GOBO` features set on group A, downloaded → **refuted on the published branch**: the features are live state and a save does not write them. It refutes the experiment, not the five-arrays reading. | refuted | §5.1 |
| GOBO-01 | 2026-08-30 | Where the gobo icon library lives: a table of 800 30-byte entries at the tail of the resource flash, each pointing at a 24×24 RGB565+alpha icon; the entry index **is** the gobo id read from `145.f2`. | validated | §3.4 |
| GOBO-02 | 2026-08-26 | The corrected experiment — features set, then `SHIFT` + tap to capture, then save → the five gobo features named. **Retracts** GOBO-01's guess at which fields they were. | device-confirmed | §5.1 |
| PAL-01 | 2026-08-27 | A palette colour (record 140) written by us and recalled → the written pad comes out at the channel, exactly. The last approximation on the intention → DMX path closes. | device-confirmed | §3.3 |
| RENAME-01 | 2026-08-30 | Eleven gobo names written outside the device and deployed → all eleven appear on their pads after a reload. Names cap at **19 UTF-8 bytes** (unmeasured here, taken from PRESET-05, so the tool refuses longer). | device-confirmed | §3.4 |
| SORT-01 | 2026-08-30 | The `f3 = 14` ranges of record 111 permuted, each keeping its DMX bounds → the page reorders exactly, and the firmware accepts a **non-monotonic** range list without re-sorting. The wire order **is** the pad order. | device-confirmed | §3.4 |

### Positions — records 150, 151 and 106

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| POS-01 | 2026-08-26 | One read-only capture, `Crowd` recalled on group A, 301 frames → tilt exact six for six; `106.f5`/`f6` are the **travel limits**. Pan missed by rounding and decoded `FAN` by the miss. | device-confirmed (limits), correlated (FAN) | §7.5 |
| POS-02 | 2026-08-26 | The discriminator: `Ceiling` stores `FAN = 50 %`, so the ramp must collapse → both halves held, every fixture at 50 % of its own band. | device-confirmed | §7.5 |
| POS-03 | 2026-08-26 | A moving head **detached** for independent control, one save → exactly two records moved; record **151** is the detached-fixture positions, and `150.f1`/`f2` are a slice of it. Its own prediction was built on the wrong model and was withdrawn before it was ever measured. | device-confirmed | §8.1 |
| POS-04 | 2026-08-26 | Two photographs of the `POSITION` editor → record 151 decoded end to end, with no measurement at all. | device-confirmed | §8.1 |
| POS-05 | 2026-08-26 | `Ceiling` recalled after the detach → **four channels changed in 2048**, and the 16-bit model turns an approximation into a measured value. | device-confirmed | §8.1 |
| POS-06 | 2026-08-26 | A second detach with a large `TILT OFFSET` → `151.f1` is the **fixture index**, and it had been there all along. Retracts a line written earlier the same day. | device-confirmed | §8.1 |
| POS-07 | 2026-08-26 | `Crowd` recalled after the second detach → **two channels changed in 2048**, exactly the pair `151[5].f1` names. Every term measured, none fitted, across four captures and 24 predicted values. | device-confirmed | §8.1 |

### The patch, and the DMX mapping table

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| MAP-01 | 2026-08-31 | Where the DMX mapping table lives → record **130**, in the project file. Closes lead L5, and the trap it cost is written into `AGENTS.md`: a record that already carries a name is not a record that was measured. | device-confirmed | §7.3 |
| MAP-02 | 2026-08-31 | Two sub-steps, two saves, one trip to the panel → the table reads in full. | device-confirmed | §7.3 |
| MAP-03 | 2026-08-31 | A channel moved away and back, then saved → **nothing is written**: a genuine write and its return leave no trace. "Touching an entry moves it" is refuted, second counter-example. | observed | §7.3 |
| MAP-04, MAP-05, MAP-06 | 2026-08-31 | Eleven saves, one variable each, a download between every one → the table is closed, and MAP-06 verifies the **write** on the device. | device-confirmed | §7.3 |
| MAP-07 | 2026-08-31 | Our encoder reproduces the factory table byte for byte, with no device at all. | validated | §7.3 |
| MAP-08 | 2026-08-31 | A file we wrote deployed onto a device holding 13 entries **in a different order** → read back unchanged: the device renormalises nothing. Deleting and ordering are ours to do. | device-confirmed | §7.3 |
| MAP-09 | 2026-08-31 | The ten remaining functions in one save → the table is complete, and the writer covers all of it. | device-confirmed | §7.3 |
| MAP-10 | 2026-08-31 | Ten entries **created** by our encoder, deployed → accepted **at the byte**, not merely "a file that loads". Deliberately using values the factory never writes, so a wrong field would be visible and refutable. | device-confirmed | §7.3 |
| MAP-11 | 2026-08-31 | The return to factory, produced by the show compiler from the device's own state → record 130 identical byte for byte to the factory table. | device-confirmed | §7.3 |

### Failures of the device, and of the data

Two findings that carried no id until the ledger was written. They are numbered
here for the first time — a new number for something previously unnumbered, not
a renumbering of anything.

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| BUG-01 | 2026-08-25 | A real blackout on the operator's rig, diagnosed from the file alone → record **120** emptied for every fixture but the first, 251 of 258 entries reduced to `2a 00`, which reads back as 0. Matches a documented 2.0.x bug family and the 2.0.18 changelog entry that fixes it. The patch itself is intact. | erasure observed, cause hypothesized | — |
| LOSS-01 | 2026-08-31 | Four projects vanished from the controller between the 30th and the 31st, **outside any run of this harness**, with no delete command. The harness's own snapshots from 25 August gave them back, hashes intact. No reading is offered: the device is not perfectly healthy, and that is all the traces support. **Later the same day** (SPLIT-01) the controller reported 4 projects again, the missing one among them at its August version: the loss was **not permanent**, which explains nothing but is a fact the entry lacked. | observed | — |

### The USB protocol

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| MODE-01 | 2026-08-25 | `watch-mode` polling `GET_SETTINGS` while the operator walked the front panel, **zero writes** → 19 modes named in firmware 2.0.18, 14 device-confirmed. `SMOKE` and a single `BPM TAP` change no mode. | device-confirmed | §10.2 |
| SETP-01 | 2026-08-26 | `SET_PRESET` payload discovery, one encoding at a time → recall by id works, missing ids are silent no-ops. The reading of *what the payload is* was wrong and is repaired by RAW-01. | device-confirmed | §10.2 |
| SETP-02 | 2026-08-26 | Four candidate reload events → **no protocol event replaces the live copy**. Judged on a discriminator later refuted (RECALL-01); re-proved by RELOAD-03 on a repaired one. | device-confirmed, via RELOAD-03 | §10.2 |
| RELOAD-01 | 2026-08-26 | Static read of WTOOLS 2.0.2, then an attempted USB capture → **the capture is not available on this machine** (`XHC20` refuses to come up). The static read gave names only, `[observed]`. | observed | §10.2 |
| RELOAD-02 | 2026-08-27 | A WTOOLS push, judged on the repaired discriminator — counting distinct appearances over 10 `SKIP_PRESET` → a push **does not** make the pushed project live. | device-confirmed | §10.2 |
| RELOAD-03 | 2026-08-27 | The four SETP-02 negatives replayed on the repaired discriminator, under the same UUID → they hold. The original result was not an artefact. | device-confirmed | §10.2 |
| RELOAD-04 | 2026-08-27 | Verdict for the session: **no reachable event reloads the live copy**. Only a manual open on the panel does — and that screen (mode 26) *is* reachable remotely, which is the consolation prize. | device-confirmed | §10.2 |
| RELOAD-05 | 2026-08-27 | The instrument calibrated on **both** arms: the discriminator had never been seen to fire, so the 6-entry file was opened by hand and the series replayed. | device-confirmed | §10.2 |
| RECALL-02 | 2026-08-27 | One continuous DMX stream with timestamped transitions, on the very project SETP-01 was measured on → what event 41 actually does. Kills "f1 = 22 → Deep Green" outright rather than merely contesting it. | device-confirmed | §10.2 |
| RECALL-03 | 2026-08-27 | Bytes recalled one at a time against a live copy whose ids and positions diverge only at the tail → the byte is the **id**, not the position. The position reading is refuted twice over. | device-confirmed | §10.2 |
| RECALL-04 | 2026-08-27 | A live copy of 6 entries, ids 0…5, so every id ≥ 6 is absent → **an absent id does nothing**, and the two rival readings predicted opposite frames. | device-confirmed | §10.2 |
| RECALL-05 | 2026-08-27 | The **interior** hole, which RECALL-04 could not reach: every absent byte it fired was above the highest present id → an interior hole does nothing either. A single-variable differential in the same shot showed a recall does not touch the position layer. | device-confirmed | §10.2 |
| RECALL-06 | 2026-08-27 | Ids ≥ 128, the last unprobed range of the byte → recall is exact beyond 127. Refutes, a second time, the idea that something masks the high bit. | device-confirmed | §10.2 |
| RAW-01 | 2026-08-27 | Found by sweeping `SET_MODE`'s field numbers → **the payload is not protobuf**: `payload[0]` is the index, and the device had been reading our *tag* byte all along. Re-reads the RECALL-01/02 retractions: the addressing existed, our encoding hid it. | device-confirmed | §10.2 |
| RAW-02 | 2026-08-27 | Five payloads per event, mode re-read each time → the **second byte is not read**. | device-confirmed | §10.2 |
| MODE-39/40 | 2026-08-27 | Raw indexes 39 and 40 sent from HOME, with a COLOR FX escape after each → 39 reports 39 and leaves the screen alone; 40 lands the device on mode **42**. The behaviour is device-confirmed; the *names* are hypothesized by elimination. | device-confirmed (behaviour) | §10.2 |
| MODE-40/42 | 2026-08-27 | Index 40 → the **USB STICK** screen, with project, fixture and full-backup import/export. Reachable only by raw index. | device-confirmed | §10.2 |
| PROJECTS-01 | 2026-08-27 | Index 26 → *main menu → Open*, with the list of six stored projects on screen. This is the screen that reloads, and it is reachable remotely. | device-confirmed | §10.2 |
| SCREEN-01 | 2026-08-27 | A sequence of indexes at 5-second intervals, operator watching → **the reported mode is not the displayed screen**. One clause of the entry was retracted the same day. | device-confirmed | §10.2 |
| SCREEN-02 | 2026-08-27 | How to leave a modal screen, and what that says about the menu. | device-confirmed | §10.2 |
| SCREEN-02 | 2026-08-31 | Which indexes actually move the panel → **pushable {1, 3, 4, 26}, inert {0, 5, 16}**. The *target* decides, not the screen you start from; "only modals are pushable" was refuted by indexes 4 and 5, and one of the six measures refuted its own prediction. WTOOLS uses the same `setWMMode`, so there is no private channel. | device-confirmed | §10.2 |
| SCREEN-03 | 2026-08-27 | The three indexes that would not move the screen, replayed on **another live copy** → the panel yields to keys, not to `SET_MODE`. Two distinct paths, and nothing has yet measured which one the panel follows. | device-confirmed | §10.2 |
| LINK-01 | 2026-08-27 | Seen five times in one day, always identically: after a pause, or after another process held the port, the **first** request times out and the next succeeds. | observed | §10.2 |

### The resource-flash upload

Four readings taken on 2026-08-31 from files already on the machine — the five
retained WTOOLS logs and the installed bundle. **No device operation, no byte
sent, no port opened.** They are here because they are the only state known of
the channel that writes the resource flash, and because one of them contradicts
something this repository publishes as `device-confirmed`.

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| UPLOAD-01 | 2026-08-31 | The retained WTOOLS logs read on this machine → the flash upload travels on the **same tty as everything else** (`SerialPortAgent : Opened serial port (/dev/cu.usbmodem…) as read/write`), not on a raw USB pipe. RELOAD-01 went looking for a USB capture and found this machine could not make one; the channel it wanted to watch is a serial port, and the vendor's own log is the instrument. RELOAD-01's measurement stands — nothing is refuted — its **orientation** is superseded. | observed | §10.2 |
| UPLOAD-02 | 2026-08-31 | The two sessions that carry an upload, counted and timed → **180** `setFlashDataChunks` messages each, the same shape twice: one message, refused, then **179** at a 163 ms median. `wolfmixFlash.bin` is 2 931 442 bytes, and 16 384 is the only candidate chunk size that divides it into exactly 179 messages (178 full, then 15 090). Read as one control message plus 179 chunks — the log names message *types*, never payloads or lengths, so nothing here has seen a chunk. Prediction published before the measurement that will settle it, in `corpus/experiments/UPLOAD-02/prediction.md`. | hypothesized | §10.2 |
| UPLOAD-03 | 2026-08-31 | The three binaries of the installed bundle hashed against the `files` list of the `changelog.json` shipped inside it → **three matches out of three**. The factory image is present and provable, so a copy taken outside the WTOOLS folder is a verified restore point and not a copy of whatever was there. Local integrity only: the manifest travelled inside the same bundle and says nothing about what the vendor's server served. `gobo_run.py`'s gate 0 re-runs the comparison on every run. | observed | — |
| UPLOAD-04 | 2026-08-31 | The two upload sessions read against what `docs/gobo-icons.md` §5 publishes → both were **refused at their first message** (`UNSAVED_PROJECT`, *Save project before updating*), and the 179 that followed came after the operator saved and clicked again; yet the upload itself is published `device-confirmed` for the same day, on the magenta-icon write and its undo. The disagreement is written rather than resolved: nothing here establishes what the device does with a refused first message, and no claim is added to §5 about what it keeps. | observed | §10.2 |

The boundary question stays **open**: nothing above establishes whether
executable firmware and the resource flash travel on the same message.
`tools/wolfmix.py` gains no event, and this repository still writes no flash of
its own.

### Reading a fixture profile off the controller

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| PROFILE-01 | 2026-08-31 | `GET_PROFILE` (event 3), allowlisted since the start and never called, sent with the same UUID payload `GET_PROJECT` takes. Prediction published first: same payload (0.6), a different field layout (0.25), a refusal wanting an index (0.15) → **the first held**, and the reply is plain protobuf. `f1` UUID, `f2` name, `f3` the body, `f4` version in ms epoch, `f5` brand; in the body, `f1` per channel, `f3` per preset band, `f4` the mode table. The consequence is the point: the `.wmx` fixture library on disk is encrypted with no public key, and the controller hands the same content over **in the clear**. The layout now lives in `PROFILE_FIELDS` and `decode_profile`, not in prose. One profile family, three reads — swept by PROFILE-02. | device-confirmed | §10.2 |
| PROFILE-02 | 2026-09-01 | The sweep PROFILE-01's risk table asked for: **221 profiles across 60 brands**, 1 to 58 channels, 27 of them carrying two gobo-wheel channels, decoded and checked for fields the tables do not name → **no `unknownFields`, anywhere**, and every channel type in all 221 lies inside `ssl2.TYPE_CANAL` — the enum the controller and the `.ssl2` writer share, corroborated well past the eleven values PROFILE-01 had checked. Three of PROFILE-01's sub-readings were **refuted** by the same sweep: the body's `f2` link table is **not ×16** (16 in 15 profiles out of 221; it holds one entry per mode-channel, `len(f2) == Σ modes[].channelCount` in 221/221), the body's `f5`/`f6` are **not always zero** (one profile reads 6400 / 2358, two omit both), and the mode table's `f3` is **not always 1** (15 distinct values over 498 modes). None of the three is named in the decoder, so each surfaced as a value under its neutral `fN` key rather than as a wrong name — which is what the neutral keys were for. | device-confirmed | §10.2 |
| PROFILE-03 | 2026-09-01 | The preset `f4` disagreement, measured over **13 291 preset bands** of the sweep: `research/versions.md` read `f4` as the channel type of what the band acts on, `ssl2.CIBLE_PRESET` maps a `.ssl2` **preset** type to a target channel type. → **two different axes, neither reading wrong.** `f4`'s domain is 26 values, every one of them in `TYPE_CANAL`, and **not one** of the 17 numbers that are in `TYPE_PRESET` but not `TYPE_CANAL` ever appears — so `f4` is a channel-type number, not a preset type. It equals its own channel's declared type in 82.6 % of bands and names the band's function in the rest (a `Generic` channel whose band is a colour selection, a `Prism Rotation` channel whose index band reads 21 `Prism Index`). `SSLPRESETTARGET` is a channel **index** within the mode, resolved by `CIBLE_PRESET`'s rule — a different question from what `f4` answers. **No equivalence is encoded in either tool**, and none should be: `CIBLE_PRESET` stays the 97.4 % statistical derivation it was. | device-confirmed | §10.2 |
| PROFILE-04 | 2026-09-01 | Whether `GET_PROFILE` serves the locally-authored profiles whose identifiers are derived from the device serial, or only the vendor library → **it serves them**, both of them, decoded by the same tables with no `unknownFields`. They are visibly written by a different encoder: every zero-valued scalar is omitted, so channels come back as `{}` or `{"index": 1}` where a vendor profile writes explicit zeros. Their brand also **disagrees between two events**: `GET_PROFILE_LIST` reports one brand for both, `GET_PROFILE` reports a different one for the first and none at all for the second. Recorded, not resolved. Neither identifier is reproduced here ([`../LEGAL.md`](../LEGAL.md)). | device-confirmed | §10.2 |
| PROFILE-05 | 2026-08-31 | Record `116.f9`, published as a "16-byte profile hash" since the corpus mining, parsed as a UUID across the whole variant-A corpus → **293 of 357 entries are RFC 4122 v5**, and the residue of 64 is the two locally-authored profiles PROFILE-04 reads off the device, whose identifiers are serial-derived. `116.f9` is the profile **UUID**: the `.wpj` and the controller share one key space, and it is the identifier `GET_PROFILE` takes. Nothing here parses as a digest of anything. **BREAKING**: the codec key `hash` is removed, not aliased — `116.f9` is `uuid`, dashed, and the corpus round-trip stays byte-exact in both directions, the two serial-derived identifiers included. No version validation: refusing a non-v5 identifier would refuse a project the operator owns. | correlated | §7 |

### Firmware provenance

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| FW-01 | 2026-08-27 | The vendor's own changelog, read on this machine → `165.f10` bit 5 was called **`DIMMER`** until 2.0.15 and `OTHER` since. The measuring firmware (2.0.18) is later than the rename. | observed | §5.3 |
| FW-02 | 2026-08-27 | Same source → `f32`–`f35` are dated to firmware 2.0.5, which dates schema 10. Three corroborations in one line, on a reading already device-confirmed. | observed | §5.3 |
| FW-03 | 2026-08-27 | The discriminator FW-01 left planned: write bit 5 **and nothing else** → `OTHER` gates the dimmer of a static cue and nothing more, on this scope. It is what lifts GEN-02's bit back from `hypothesized`. | validated | §5.3 |

### Read coverage

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| COV-01 | 2026-09-01 | Every byte of a variant-A project attributed to a named field, an unnamed one, or a schemaless record, with the three asserted to sum to the file → the corpus reads **62.4 % / 27.8 % / 9.8 %**. Most of the gap was not undiscovered but **unpromoted**: records 102, 106, 110 and 155 had full field tables in SPEC.md and no codec schema, and so did `150.f3`/`f4`/`f6`/`f7`, `165.f4`/`f11`/`f15`/`f16`/`f18` and the engine-specific `f3`/`f5` of the FX sub-message. Promoting only what the ledger already supported, with a byte-exact round trip throughout, takes it to **87.6 % / 10.6 % / 1.8 %** — 48.4 % of the file named by a device-confirmed reading, 26.6 % correlated, 12.7 % validated, and nothing named without an entry in the ledger. | correlated | §3 |
| COV-02 | 2026-09-01 | Whether `165.f27` is really zero everywhere → **refuted**, see above: 26 presets carry `[1000] × 8`, split by author (WTOOLS writes 1000, the panel writes 0). Q1's blocker — "no cheap discriminator has emerged" — no longer holds. | observed | §5.6 |
| COV-03 | 2026-09-01 | Whether record 120's entries are structurally empty on this rig → **no**: 10 754 of 14 698 carry content. The all-empty file is *rig-c-bug*, which is BUG-01's erasure. | correlated | §7 |
| COV-04 | 2026-09-01 | Field 1 as the item count, swept over every record type → exact on **15** of them, with an empty table writing no field 1 at all rather than a zero. `165.f1` is **not** the count: it disagrees on 19 files, always low by exactly the entries a preset capture appended (81 against 83, 81 against 85). Whatever it counts, it is not the entries present. Now `comptes_de_tete` in `wpj_identities.py`. | correlated | §3 |
| COV-06 | 2026-09-01 | Record 161, SPEC's Q8 — "three opaque blobs of ~190 bytes", the only record type with no schema — asked whether the blob is packed varints, the shape `155.f4` turned out to have → **it is**: exactly **188 values** in all 228 occurrences (76 files × 3 items), while the byte length varies 189/190/191 because some values pass 127. Reading it as a byte array works by accident, which is the same trap 155 sprang. The rest of the record is regular too: `f1` takes exactly three values, one per item position — **21, 15, 19**, constant across the whole corpus — `f2` is 1, `f4` is 1 or 2, and the record's own `f1` is **9** against 3 items, so the field-1-is-the-count identity does not hold here. The array is nearly all zeros: two or three non-zero values at the head and one at index 187, which is 160, 176 or 240. **Nothing is named**: this is the shape, not a reading, and 161 stays passthrough. But it is no longer opaque. | observed | §11 |
| COV-08 | 2026-09-01 | `150.f8`, the CROSS half of the POSITION screen — 1.90 % of the corpus and the largest unread field left, `[hypothesized]` in §3.2 and never read off a screen. **No write, no save, no deploy**: the experiment project already holds a slot where it is not neutral. Prediction published before the measurement, with the two competing readings — unsigned `v/65536` (54) against signed `(v−32768)/32767` (10) — and the collision check that neither lands on another value of the same screen. The measurement carries its own control: PAN, TILT and FAN are device-confirmed, so 60 / 7 / 50 on screen is what says the panel holds the state the prediction was computed from. Full sheet: [`corpus/experiments/COV-08/prediction.md`](../corpus/experiments/COV-08/prediction.md). **Measured, two slots, five photographs, nothing written.** `f8` is `CROSS` on the **unsigned** scale: the screen read **55** where the unsigned reading predicts 55 and the signed one 10, then **50** where they predict 50 and 0. Prediction 3 held, prediction 4 refuted. `FOCUS OFFSET` is confirmed signed in the same shot — 32768 shows **0 %**, not 50 %. The control held on identity rather than on value: the screen's header reads the group-A name record 125 carries, and the ALL POSITIONS page reproduces the palette slot for slot, group A's operator-named first slot against `Floor` on the others. | device-confirmed | §3.2 |
| COV-10 | 2026-09-01 | Whether `165.f27` is the per-group **position FADE**. COV-08's photographs show the `ALL POSITIONS` page carrying one `FADE` per group, printed in seconds, reading `0s` on a project whose every preset has `f27 = [0] × 8`. 1000 would be 1 s, `f11` and `f15` are already milliseconds, and `f27` sits immediately before `f28`, the per-group position index — two per-group tables side by side. The measurement **writes nothing**: `WMX EXP05` is on the controller with `[0] × 8` and `[1000] × 8` on two adjacent pads, so two recalls and one screen settle it in both directions. Sheet: [`corpus/experiments/COV-10/prediction.md`](../corpus/experiments/COV-10/prediction.md). **Measured, both directions, and the unit is named.** Recalling a preset carrying `[1000] × 8` puts group A's `FADE` at exactly `1s`. Then the second point, published before the download because one point cannot separate a unit from a table: `FADE` set to **3 s** on group A alone and captured into a new preset → that preset carries `f27 = [3000, 1000, 1000, 1000, 1000, 1000, 1000, 1000]`. Three points on one line — 0 ↔ `0s` on a panel-composed project, 1000 ↔ `1s`, 3000 ↔ `3s` — so the unit is the **millisecond**. The seven trailing 1000s are their own control: they are the live state the recall had just installed, captured unchanged. **One save, one preset created, not one existing preset altered by a single field.** Measured on group A only — no fixture sits on the other seven here (COV-13 records what else the save touched). | device-confirmed | §5.6 |
| COV-11 | 2026-09-01 | **`SHIFT` + an encoder puts the POSITION screen into two decimals**, found at the panel. It is also a fine adjust, so the four values of group A's first slot moved and were saved; the download shows **one record changed and one slot in it**, which makes the pair a clean single-slot differential. At 100× the resolution the display formula stops being ambiguous: PAN 35093 → `53.55`, TILT 23009 → `35.11`, FAN 32807 → `50.06` all land on **unsigned `v/65536`**, and FOCUS OFFSET 65515 → `99.94` lands on **signed `(v−32768)/32767`**, where the unsigned reading would have printed `99.97`. Four independent points, and **rounding takes 4/4 against truncation's 0/4** — COV-09 re-measured at two decimals instead of zero. The consequence is a method, not a field: every future position measurement should be read this way, because a two-decimal reading discriminates hypotheses that a whole-percent one cannot. Snapshot `COV-11`. | device-confirmed | §3.2 |
| COV-12 | 2026-09-01 | Record 150's unnamed slots are **named by the firmware**, `Position N` with N the 1-based slot index, exactly as record 145's unnamed pads print `Gobo N` (§3.4). Read off the `ALL POSITIONS` page of a project whose stored palette names slots 1, 6 and 11–14 and leaves the rest absent: the screen prints the four stored names where they exist and `Position 2`, `Position 3`, … everywhere else. The same page also confirms the archive reflects the controller — twenty slots, name for name. **A writer must not expect to find these strings**, the same trap §3.4 records for gobos. | device-confirmed | §3.2 |
| COV-14 | 2026-09-01 | `f16` slice 1 = `move_fx_active` — `correlated 45/45`, 3697 presets, asserted as an identity — **refuted** by the preset the device composed for COV-10: slice 1 = 1 against `move_fx_active` = 0. See the refutations. `slice 1 ⊇ move_fx_active` survives, exact, and still kills the stride-8 alignment on 1767 presets. Slice 0 against `color_fx_active` now fails both containments and is asserted at neither. The slice value domain SPEC gives as `{0, 1, 2, 5, 7, 255, 511}` is also stale: **3 and 4 occur**. | device-confirmed | §5.4 |
| COV-16 | 2026-09-01 | `120.f1` — record 120 is 2.0 % of the corpus and named nothing inside. Laid out in **ascending DMX-address order** (what §7 says the blocks follow, not the item order of 115) the table is **constant per profile**: four identical ten-entry blocks for one par model, two identical sixteen-entry blocks for the moving heads. Prediction published first, built only from the **26 channels record 106 gives no role**, of which eight are non-zero → **all eight exact off the live frame**: 4 on channels 108/118/128/138, 127 on 109/119/129/139, and none of the eight moves in six consecutive change frames while an effect drives ten other channels. `120.f1` is the channel's **DMX value**, emitted wherever no engine claims the channel. Prediction 2 was **wrong in a useful way**: several role-free channels are *not* idle, because a 16-bit pair is driven on both halves while only its principal appears in 106 (POS-05) — "no entry in 106" is not "not driven", and a future test must exclude fine channels too. `f4`, `f5`, `f6` stay unnamed. Sheet: [`corpus/experiments/COV-16/prediction.md`](../corpus/experiments/COV-16/prediction.md). | device-confirmed | §7 |
| COV-17 | 2026-09-01 | `120.f5` and `f6`, hunted after COV-16 named `f1`, and **not named**. Three hypotheses tested on the corpus and all three fail: `f5` is not the engine role of its channel (557/2228), `f6` is not the index of the `k XOR 1` partner in 120 (131/2157), and `f5` is not cleanly coarse/fine against `110.f5` — 986 principals against 128 fine at `f5 = 1`, 898 fine against 216 principals at `f5 = 2`. **89 % and 81 % is a correlation, not an identity**, and a name resting on it would be the failure mode this repository counts as worse than an unknown field. What is established is the shape: the two fields appear only in small clusters, `f5` ∈ {1, 2} only, and `f6` runs in consecutive quadruples whose per-fixture base advances by exactly the profile's channel count. The partner-swap is visible by eye in both files examined and is *not* reproducible by any index formula tried. | observed | §7 |
| COV-18 | 2026-09-01 | What record 160 actually is. The name "macros" came into this repository as **`correlated (pre-existing)`** — inherited from an external reading, never measured, and it is the only record whose name has that provenance. Two independent things attach it to **`LIVE EDIT`**: the firmware's own mode table has `live_edit` (10), `live_edit_edit` (15) and **`live_edit_macro_edit` (41)**, so the word is the vendor's and it is a Live Edit word; and the **9 corpus files with no record 160 carry no `165.f18` bit anywhere**, 9/9, while every file that has the record has bits. The bits set across the corpus are `{0, 1, 3, 4, 19}` 0-based, which is exactly §5.5's "buttons 1, 2, 4, 5 and 20". A macro is a **Live Edit button**, and its stored name is the operator's label. Two further guesses were tried and **fail**: the bits do not index the macro list (7 files of 71 set a bit past the macro count), and they are not the set of `160.f5` values. `f8` is the only field with an obvious shape — one value repeated, 50 / 128 / 255 / 43 / 93 on five named macros, which reads as a DMX value — and it stays unnamed until something writes a known one. | correlated | §3 |
| COV-19 | 2026-09-01 | Record 160 measured, after COV-18 identified it. A Live Edit macro created at the panel on the button labelled **`Live Edit 6`**, feature `01 Pan`, value **200** — a value present nowhere in the corpus, so it cannot be inherited — then saved. **One record changed and one only**, 160, 393 → 416 bytes, one macro appended. It carries `f8` = **[200, 0]** and `f5` = **5**. So `f8` holds the values and `f5` is the **button index, 0-based**: the panel's "Live Edit 6" is stored as 5. `f6` is absent, and the panel synthesises `Live Edit N` — the third record after 145 and 150 to do this (COV-12). Three identities then hold on all 71 files that have the record, and are now checked: the buttons in a file are **distinct**, all fall inside the 4 pages × 20 = **80** grid, and **every bit any preset sets in `165.f18` lands on a button some macro defines** — a preset cannot carry a Live Edit that is not defined. The wire order is free, 65 of 71 files storing them out of button order, like record 111's gobo pads (SORT-01). `f1` and `f7` stay neutral: no reading. Snapshot `COV-19`. | device-confirmed | §3, §5.5 |
| COV-20 | 2026-09-01 | `160.f1` and `f7`, opened by COV-19's measured macro. `f1` is a list of **bit masks** and `f8` gives **one value per set bit**, in order: `popcount(f1) == len(f8)` on all **490** corpus macros, no exception, and it is now an identity. The measured macro is the smallest case and shows the mechanism bare — one targeted fixture, `f1 = [17]` (two bits), `f8 = [200, 0]` (two values). `len(f1)` never exceeds the fixture count (490/490), which fits **one mask per targeted fixture**; that is consistent and not measured, so the name says what the identity says and no more. The identity also **refutes** reading `f8` as a per-group array of eight, which is exactly what its `[50] × 8` shape on the factory macros invites. `f7` stays unnamed: its length is `2 × len(f1) + 4` on 489 of 490 — a shape with one exception, and no reading. | correlated | §3 |
| COV-21 | 2026-09-01 | Record 161 — SPEC's Q8, "three opaque blobs", called **volatile** — swept across every corpus project that has more than one version. It is the opposite of volatile: of the **564 packed values** (3 items × 188, COV-06), exactly **6 ever differ**, and they are the *same six* in two unrelated projects. They are the **last value of each of the three items** — which always move **together and to the same number**, 240 or 176 — and the **first three of item 1**, which read as a little-endian **20-bit mask**, `0x0FFFFF` (all set) or `0`. The other 182 values of every item have never moved in 82 files, 13 projects and two authors. The two transitions visible in one project's history are F30-04 (the shared tail 240 → 176) and FX6-02 (the 20-bit mask all-set → zero, on a save whose stated variable included `LIVE EDIT` being off). **Nothing is named** and 161 stays passthrough: two transitions are not an attribution, and a 20-bit mask has too many candidates on this device — a palette page, a preset page, a Live Edit page are all 20. What is gone is the word "opaque": the record is a near-constant with six moving parts and a shape (COV-06). | observed | §11 |
| COV-22 | 2026-09-01 | The 7th byte of `125.f4`, which §7.2 records as having "flipped `0x30` → `0x38` within one project when the device added a preset". **The attribution is refuted, twice.** COV-10's save created a preset and the byte did not move (it was already `0x38`); and a second project runs `0x38` → `0x30` → `0x38` across three versions with its **preset count constant at 82** — so it moves without a preset being added, and it moves **back**, which no one-way flag does. What survives, and is new: it moves **in the same save** as the shared tail of record 161 (COV-21), in both projects where more than one version exists — `0x30`/240 and `0x38`/176 in one, `0x38`/160 then `0x30`/240 then `0x38`/176 in the other. The pairing is not a bijection (`0x38` appears against both 160 and 176), so the two are **coupled without being derivable from each other**. Neither is named. | observed | §7.2 |
| COV-26 | 2026-09-01 | `125[i].f4` == the OR of `1 << 115[r].f3` over the slot's fixtures — `correlated 45/45`, "fully derived", asserted as an identity — **refuted** by a file the device wrote. Reordering record 116 changes every profile index, and the firmware **ORs the new bit in without clearing the stale one**: a slot whose two fixtures give `0x6` stores `0xe`, keeping a bit from the ordering before. The mask is therefore **not recomputed from the patch, it accumulates**, and §7.2's "recomputable from 105 + 115 + 116" is wrong in the one direction that matters to a reader — it may claim profiles the group does not have. What survives is `stored ⊇ derived`, exact on all **747** corpus slots, and the identity now checks that. Second time in one day an equality that held on the whole corpus fell to a file written after it (COV-14 was the first), and both times for the same reason: nothing had ever exercised the case. | device-confirmed | §7.2, §7.4 |
| COV-29 | 2026-09-01 | COV-23's measure 4: one fixture's DMX address moved 500 → 300, on the cleanest case in the patch — a **one-channel** fixture with no neighbour, so nothing can overlap. **Exactly three records changed, all at constant length**: 115, 106 and 120, as predicted. In 115 only `dmx_address` moved (499 → 299); in 106 only `dmx_channel`, the entry otherwise byte-identical including its `limit_low`/`limit_high`. In record **120 not one entry changed** — the only difference is the record-level `f4`, whose 13 packed varints reorder their tail. So **`120`'s per-entry `f4`/`f5`/`f6` do not depend on the patch**: they belong to the profile, which refutes COV-23's prediction 3 that `f6` would follow the fixture's new rank in address order. **Caveat, stated because it matters:** the moved fixture's own 120 entry and its new neighbour's are both empty, so a block that moved would be invisible at this resolution — the finding is that the *named* fields do not follow the patch, not that no block moved. The record-level `f4` is the one thing an address move does write, which is the first attribution it has ever had. **The return is byte-perfect:** moving the address back to 500 restored **every record** identically, the record-level `120.f4` included, with only the version counter and the SHA-1 moving. So `120.f4` is a pure function of the address ordering with no hysteresis — which is worth stating next to COV-13, where an unrelated save canonicalised two records it was not asked to touch, and COV-26, where a mask accumulated instead of being recomputed. The same firmware does all three. | device-confirmed | §7, §7.1 |
| COV-28 | 2026-09-01 | What `160.f7` says about the fixture COV-27 proved it encodes. A third macro, same profile, same feature, same value, **only the fixture differs** — the second moving head instead of the first. Sheet: [`corpus/experiments/COV-28/prediction.md`](../corpus/experiments/COV-28/prediction.md). **One record changed, 160 alone.** Predictions 1 and 2 held exactly: `button` = 6, and `value_masks` = `[68]` with `values` = `[60, 0]` **byte-identical** to COV-27's, so the feature side does not move when the fixture does. Prediction 3 held: `f7` **differs**, `04 81 01 7f 7f 7f 7e` against `81 01 7f 7f 7f 7f 03`. **Prediction 4 is refuted**: none of the three pre-registered candidates appears — not the record-115 index (14 vs 15), not the DMX address (1 vs 17), not `f9` (20 vs absent). What moves is a leading `129 → 4` and a trailing `3 → 126`, and the six-varint length is unchanged. So `f7` identifies the fixture by something that is **not** any of the three identifiers the file already carries for it, and it stays unnamed. The factory macros' shape is consistent with this — an eight-fixture macro carries eight `(x, 129)` pairs and a four-varint tail — but consistency is not a reading. | device-confirmed (that it differs); the encoding refuted as index, address or `f9` | §3 |
| COV-27 | 2026-09-01 | COV-23's measure 2, run clean: a second Live Edit macro, **same single fixture**, **one** feature — `02 Tilt` = 60 — on the free button the panel calls `Live Edit 3`. **One record changed**, 160 alone, 416 → 438 bytes, one macro appended on `button` = **2**, 0-based as COV-19 predicted. `f7` came back **byte-identical** to the Pan=200 macro's: `[129, 127, 127, 127, 127, 3]`. Two macros, same fixture, different features, same `f7` → **`f7` encodes the targeted fixture, not the features.** That was the pre-registered discriminator and it fired. Two of the sheet's predictions were **wrong, and usefully**: `values` is `[60, 0]` and not `[60]`, and the mask has **two** bits and not one, because the panel takes the **fine channel along with the coarse one** — `01 Pan` drags `03 uPan`, `02 Tilt` drags `04 uTilt`. That also explains COV-19's `[200, 0]`, which had looked like a stray zero. The two masks are `17` and `68` = **17 ≪ 2**, so a feature's bits shift by two per feature; two points, and nothing is named on them. `f7` stays unnamed too: one fixture is not a mapping. | device-confirmed | §3 |
| COV-23 | 2026-09-01 | Four measurements planned and published before any of them, aimed at the fields the corpus sweep could not move: `FIXTURE SETUP` for `115.f5`/`f9`, a one-feature macro for `160.f7`, `LIVE EDIT` re-enabled for record 161's 20-bit mask, and a DMX address moved for `120.f4/f5/f6`. Sheet: [`corpus/experiments/COV-23/prediction.md`](../corpus/experiments/COV-23/prediction.md). **Measure 1 was run and its one-change-per-save condition was not met** — the session that produced it moved through the setup screens and saved once at the end, touching eleven records: the patch was reorganised, fixtures were added, and record 151 fell from 93 bytes to 16. The prediction "record 115 changes, and only for that fixture's row" is therefore **untested**, not refuted. What the same save did yield, by value rather than by position, is COV-24 and COV-25. Measures 2, 3 and 4 are still open. | planned; 1 of 4 run, inconclusive | §7.5 |
| COV-24 | 2026-09-01 | The `FIXTURE LIMITS` screen is `106.f5`/`f6`. Photographed before and after an **invert pan** on one fixture: the screen read `MIN PAN 52 %, MAX PAN 79 %, MIN TILT 0 %, MAX TILT 56 %`, and the file's `limit_low`/`limit_high` on that fixture's pan and tilt entries read **133, 201, 0, 142** — `v/255`, four exact points, of which two (79 and 56) are **rounded** and not truncated, a third independent confirmation of COV-09. After the inversion the same entry reads `limit_low = 201, limit_high = 133`: **invert pan swaps the two**. This puts a screen and a name on what POS-01 had confirmed through DMX only. **The save was not single-variable** — eleven records moved, the patch was reorganised, and record 151 fell from 93 bytes to 16 — so nothing else in that diff is attributed, and the limits are read by value, not by position. | device-confirmed | §7.5 |
| COV-25 | 2026-09-01 | `110.f4` is the channel type the `FIXTURE BUILDER` screen shows, name for name. A 16-channel moving head decodes as Pan, Tilt, Pan, Tilt, Speed, Frost, Shutter, Dimmer, Color Wheel, Generic, Gobo Wheel, Gobo Shake, Focus, Prism, Prism Rotation, Generic against the screen's `01 Pan … 16 Reset` — **16 of 16**, the two `Generic` landing on the two entries the W1 labels `Color Effect` and `Reset`. Reading it needed `116.f3`, the profile's offset into 110, which §7 has documented since the corpus mining and the codec **had never named** — without it every profile decodes from offset 0 and returns another profile's channels, which is how this measurement first came out nonsense. **Warning, and it is the point of this entry:** `ssl2.TYPE_CANAL` agrees with these numbers up to 27 and then **diverges** — a `RGBAW UV` par's three extra colours are 31, 32, 33 here, where the `.ssl2` table reads 31 White, 32 Gobo Bounce, 33 Framing Rotation. PROFILE-02 established `TYPE_CANAL` for profiles read off the controller; it does **not** transfer to `110.f4`, and §7.5's neutral `extra1/2/3` is the right name for 31/32/33 until something measures them. | device-confirmed | §7.5, §7.6 |
| COV-15 | 2026-09-01 | `115.f9`, which §10 reads as "the fixture's **sequential index** (0 omitted, then 1…19)" from one rig's canonicalisation → **the index reading is wrong**. It is a contiguous ascending run in 68 of 80 corpus files, but the run does not always start at 0: a six-fixture project carries **20…25**. A position index cannot exceed its own count. What it is — a creation counter, a stable per-fixture identifier — is not settled here, so nothing is named; `f9` keeps its neutral key. The 12 files with no run at all are the rig-a family, where the field is simply absent. | correlated | §10 |
| COV-13 | 2026-09-01 | Side effects of the COV-10 save, recorded because a "single-variable" claim has to survive looking at the rest of the file. One save, version 5 → 6, **one preset created and not one existing preset changed by a field** — but two other records moved, both canonicalisations rather than edits. `115`'s six fixture entries each **gained `f6 = 2`**, absent before; and the seven empty gobo palettes of records 145 went from 123 bytes to 42, i.e. from the "empty slot" form (`f1 = ' '` × 20) to the "wholly empty" form (`2a 00` × 20). §3.4 documents both forms and requires a writer to reproduce whichever it read; what is new is that **a controller-side save converts one into the other**, which is a second instance of the rule §10 already states — a device save is not byte-preserving. Neither touches record 165, so COV-10 stands. | observed | §10 |
| COV-09 | 2026-09-01 | The published COV-08 prediction was **wrong by one** on six of eight values, and the cause is a rule this repository applies everywhere: "the display truncates". On the POSITION screen it **rounds**. Seven of the eight readings discriminate between the two, and rounding matches **8/8** against truncation's 1/8 — 39976 is 60.9985 % and shows **61**, 32767 is 49.9985 % and shows **50**. The reading of `f8` survives untouched because its two candidates were 55 and 10, which no ±1 can bridge; a measurement whose rivals sat one apart would have been decided by the wrong rule. Truncation stays established on the **colour** picker (§3.3, 127/255 → 49): either the two screens format differently, or one of the two measurements needs redoing. | device-confirmed | §3.2 |
| COV-07 | 2026-09-01 | COV-02's author split applied to the twelve `f16` slices, to see whether a WTOOLS-written project fills what the panel leaves empty → **it does not**, and that is the useful part. Slices **3 and 4 are zero on both sides**, so Q4's "empty in everything reachable from this panel" now also covers everything reachable from WTOOLS 2.0.2. Slice 11 reads {2, 5, 7} from the panel and {2} from WTOOLS — no new contrast. Slice 5 is 255 on every WTOOLS preset, consistent with GEN-03's reading that the panel is what narrows it to the groups a cue lights. A negative result, published because the method that worked on `f27` was tried here and failed. | observed | §5.4b |
| COV-34 | 2026-09-01 | A third-party guide of **unknown provenance** claims the W1 rebuilds its output every **40 ms (25 fps)**. The same document is wrong where this repository has measured — it says the gobo palette is derived in real time and not stored, which EXP-06 refuted and RENAME-01 contradicts `device-confirmed` — so it is a source of leads and not of facts, and this is the cheapest of its claims to test. **Planned and published before the measurement**, sheet: [`corpus/experiments/COV-34/prediction.md`](../corpus/experiments/COV-34/prediction.md). `dmx_envelope` now reports the **distribution of frame arrival gaps** and not only a count, because an average cannot separate one frame every 40 ms from one every 20 ms with every other dropped. Three rival periods are pre-registered — 40.0 ms (a fixed engine tick, p 0.45), 22.7 ms (one USB frame per DMX512 frame at 250 kbit/s, p 0.20) and 33.3 ms (p 0.15) — the closest pair 6.7 ms apart against a window yielding 700 to 1300 gaps. **Stated in the sheet and to be stated in the result: this measures the cadence of the USB stream, not the DMX line.** The outputs are physically unplugged and there is no receiver here. Two 30 s windows, one variable — engine static against engine animating — because if the rate moves between them the stream is emitted on change and says nothing about a tick, which would make every envelope this repository has taken a measurement of the emission rule rather than of the output. **Measured, three windows.** A 5 s probe, then 30 s with the engine **static** and 30 s with a Move FX and a Colour FX running — the only variable. **25.04 / 25.04 / 25.01 fps**, median arrival gap **40.023 / 39.991 / 39.870 ms**. Prediction 1 held: the median is **0.01 ms** from 40.0, against 6.6 ms to the 30 fps rival and 17.2 ms to the per-DMX-frame one. Prediction 2 held far tighter than its 5 % bar — the two 30 s windows are **0.12 %** apart on rate and **0.30 %** on median, so the cadence does not care what the engine is drawing. Prediction 3 held by an order of magnitude: p75 − p25 is **1.124** and **0.652 ms** against a bar of 8. Prediction 4 held, 2048 channels every frame. **Prediction 5, the rival, is refuted and it is the one that mattered**: the static window ran 30 s with `animatedChannels = 0` and still shipped **751** frames — the controller emits on a clock, not on change. The decimation reading (20 ms with every other frame dropped) is excluded too: the gap counts are **100.16 %** and **100.03 %** of an exact 25.000 Hz clock, so nothing was lost, and the tail sits at 2 × the median — `min + max` = 80.55 ms against 2 × 39.991 = 79.98 — which is one delivery slot missed and caught up on the **host**, with no cluster of gaps anywhere near 20 ms. So the guide's number is right, on the link. **The claim stops there:** this is the cadence of the USB stream, the DMX outputs were physically unplugged and no receiver saw the line. What it does buy is a **measured basis for the envelope oracle** `research/` uses as a differential instrument everywhere: a window of N seconds is 25 N frames whatever the engine is doing, which is what makes "did this change alter the output" a fair question. | device-confirmed (the USB cadence) | §10.2 |
| COV-41 | 2026-09-01 | **An absent `115.dmx_address` is address 0, not an unpatched fixture** — and I had written the opposite three times. The clean project the operator created shows a freshly patched four-fixture group whose *first* row carries no address field at all, exactly like the experiment rig's rank 0; the panel numbers that one `001`, and base 0 has been established since EXP-06. Confirmed on the wire from COV-33's capture: channels **0-3** carry a static 16-bit pan of **44317** and tilt of **12042**. That fixture is not silent, it is at DMX 0. **And it hands COV-36 a second exact point**, on a different term of the ramp (rank 0, not 1) and on **inverted** travel limits (pan 201 down to 133): `PAN + etalement(FAN, 0, 6) − 0.5` predicts **44317**, delta **0**, where the old formula that ignores `PAN` gives 44666 and misses by 349. Tilt lands within one unit. What the correction costs is only the *reason* given for `n = 6` — "the unpatched ones included" described a fixture that does not exist, and `n` is simply the group's fixture count. Both self-check assertions are in `wpj_position`. | device-confirmed | §7.5, §8.1 |
| RESTORE-01 | 2026-09-01 | **The experiment project restored from its last clean version, and the five detached fixtures are back.** Not a measurement — an operation, recorded for provenance like REPAIR-01. All **75** captures of the project were swept against the 24 identities and the 5 anomaly checks in version order: the last clean one is `COV-19/after-live-edit-200.wpj`, version `…321`, **one save before the COV-23/24 session** — not the 25 August snapshot the plan assumed, and it passes the three checks written **after** the damage was diagnosed (COV-48, COV-50, COV-57), which is what made *put it back* and *put it right* the same operation again. Chosen over REPAIR-01's surgical fix on one fact: the surviving `151` entry is not corrupt data but a **correct entry whose index space shifted under it** — it names fixture 2, which was a `Lyre ZQ02244` at `…321` and a ten-channel par with neither pan nor tilt by `…327`. All six entries are coherent at `…321`, so the repair would have written off six recoverable offsets. **The write path had to be re-initialised, and that is the trace WTOOLS left**: the store of 2026-09-01 replaced the derived UUID, so `deploy` refused — correctly — and the state directory was moved aside for a clean `init` rather than the guard being edited around. Every one of the controller's five projects was downloaded and hashed first, read-only, before anything was written. **Result: all twenty records byte-identical to the source**, only the prefix differing on a fresh version counter; `151` back to **6** entries on group-A moving heads, `150` claiming 6 against 6, `120.f1` = 215 for 215 entries, **0 identities broken and 0 anomalies** on a project that carried three. Cost, and it is what the operator chose: the two macros of COV-27/COV-28 and COV-24's inverted pan are gone from the device and will be redone at the panel — all three are in the corpus with their values. **Prediction, published before the panel is looked at**, since opening the project is the next step and COV-37's *the firmware refuses a slice table that outruns its record* is still only a reading: the `FIXTURE` encoder should now show **six** detached fixtures and not none — position slot 1 carrying fixtures at DMX 32, 48, 0 and 16 at `+39/+9`, `+26/+9`, `+1/0` and `+3/+6` %, slot 5 the fixture at DMX 0 at `+13/−50` %, and slot 4 the one at DMX 32 at `0/+91` %. If they appear, COV-37's reading is confirmed in the useful direction; if they do not, the refusal is not about consistency. **Measured at the panel, and the prediction held: the six are there, with the offsets the file stores, read on the operator-named first slot of group A.** So **COV-37's hypothesis rises from a reading to a measurement** — *the firmware refuses a slice table that outruns its record rather than surfacing what it can* — and it is confirmed in the direction that matters, by making the table consistent and watching the whole table come back. It also closes COV-33's finding from the other side: the panel showed **no** detached fixture anywhere while one entry sat in the file under a table claiming six, and the same panel shows **six** the moment the claim and the record agree. sha256 of the read-back `90d2dd78645dce54…`. | device-confirmed | §8.1 |
| COV-63 | 2026-09-01 | **`115.f6`/`f7` is not project data — COV-58's decisive test fired, and the save that fired it had no file-level variable at all.** COV-38's step 1 was attempted first and *did not execute*: the version counter did not move, `projectChanged` was `false`, the download came back **byte-identical**, and none of the controller's five projects moved by a byte — the operator confirms the panel displayed nothing. So the discriminator was run instead, on F7-02's shape: a live-state gesture at the panel, then a save. **Corrected the same day, from the operator: it was not one save.** Several save attempts were made around a `MOVE FX2` change that was itself undone afterwards, so *one save, one variable* is **not** established and this entry does not claim it. What is established is the pair of endpoints: the counter moved by exactly **one**, so exactly one save was written whatever was attempted, and The counter moved `…327 → …328`, which settles the earlier attempt — **an executed save increments, so the first action never reached the save path** — and it reproduces F7-02 on a second project six days later: **record 165 came back byte-identical**, so the live page the operator set was not written and the file-level variable was **empty**. Two records moved and two only. **`115`, 379 → 279 bytes: `f6`/`f7` deleted from all twenty rows**, `(231, 20)` → absent, −5 bytes per row, exactly the shape COV-42 watched on four rows. A pair that vanishes on a save whose only operator input left no trace in the file is **not a property of the fixtures it is written on**, which is what COV-58 said would settle it and does; it keeps its neutral keys, because knowing what it is not is not knowing what it is. **`120.f1` 221 → 215**, at constant length: the stale counter COV-32 recorded on 2026-08-26 is **repaired by a later save**, six days on, so the content-then-counter repair COV-32 half-watched completes here and `_anomalie_comptes` clears. Three predictions of the COV-38 sheet are answered on the way: **P2 held**, **P4 held** at p 0.30, **P3 fell** — record 161's shared tail stayed at 240 and `125.f4_tail` stayed at 56, so COV-22's refuted coupling gains nothing — and the free rider **P11 held**, `120.occupancy` reading 215 occupied of 512 before and after because the entries never moved. Anomalies 3 → 2; the two that remain are the 150/151 damage, which only the restore addresses. Side observation, unattributed: the project moved from fourth to **last** in `GET_PROJECT_LIST` after being written. sha256 `c29b6ed6b88298fd…`. | device-confirmed | §7 |
| COV-62 | 2026-09-01 | **The three FX labels nobody here has measured**, planned and published before the measurement. Sheet: [`corpus/experiments/COV-62/prediction.md`](../corpus/experiments/COV-62/prediction.md). COV-61's worklist puts `bpm_division`, `link_order` and `speed_source` at the top of the `correlated` quarter with labels cross-referenced from an external project and no measurement behind them — 4 % of the corpus on trap 2. **Stage 0 writes nothing**: the clean project's single preset already stores `bpm_division` **3** on beam and colour and **2** on move, so under the published table the two screens must show **×1** and **×2** and must differ, which a corpus sweep can never test and one photograph can. Five predictions on stage 0, of which 1 and 2 are deliberately **one** measurement — a single screen agreeing with a single stored value is one point. A rider costs one recall and reaches `165.positions` (1.66 %, named beside the `f27` COV-10 measured and never measured itself): the preset stores `[1] × 8` and the slots are named `Floor`, `Center`, …, so 0-based lights **`Center`** at p 0.65 and 1-based `Floor` — and if it is `Floor`, two per-group index fields in one record disagree on their base. Stage 1 is the **write** direction and deliberately not a capture, which rewrites seventeen fields at once (EXP-07): three distinct values deployed under a derived UUID and read back, the way FLASH-08/10 confirmed the flash slices both ways. Ordering is stated against COV-60 — stage 0 first, because it writes nothing. | planned | §5 |
| COV-61 | 2026-09-01 | **The promotion worklist, and what it says about the 25.5 % `correlated`.** COV-54 raised four statuses by hand and that was the wrong shape for the job: the question *which names sit on the weakest evidence, and how much of the file do they carry* moves with every measurement, so it is now a command — `wpj_coverage.py --names` prints every read name grouped by status and sorted by weight. **The heaviest `correlated` names are not scattered, they cluster in record 165**, and three of the top six are the same shape: `bpm_division` (`f1`, **1.80 %**), `link_order` (`f4`, **1.77 %**) and `speed_source` (`f10`) carry enum labels **cross-referenced from `wpj-toolkit`**, an external project, and **nothing here has ever measured them** — the FX6 campaign of 2026-08-30 measured `f2`, `f6`, `f8`, `f9` and `f3` and never put them back on the table. That is 4 % of the corpus standing on trap 2, the one that cost the most in 2026-08-30: `f2` had carried the name *speed %* since ACC-04 and was the fade. The next three are `165.live_edit_mask` (1.96 %, an identity over the corpus and no capture), `165.positions` (1.66 %, the per-group position index, named beside `f27` which COV-10 measured and never measured itself) and `165.color_mask` (1.24 %, closed on the corpus in ACC-05 rather than generated). **One preset capture reaches the first three at once** — FX6-02's own trick, three mutually distinct values absent from the factory vocabulary in one save — and it is the cheapest 4 % on the board. No claim is made here about any of them; this is an inventory of where the evidence is thin, which is the half of \"100 %\" the read percentage does not measure. | correlated | §3 |
| COV-60 | 2026-09-01 | **What a slot of `160.f7` indexes**, planned and published before the measurement. Sheet: [`corpus/experiments/COV-60/prediction.md`](../corpus/experiments/COV-60/prediction.md). COV-56 read the container and stopped; this is the 0.47 % that is the biggest number left on the `partial` list after COV-59. **The corpus cannot answer it and one macro can**: every rig in it patches its moving heads at multiples of 16 in display order, so `address // 4` and `display position × 4` have been the same number for every macro ever measured — and the clean project is the one place where they are not, because COV-46 deleted fixture 0 and the addresses now start at 16 while the display positions still start at 0. One macro, `01 Pan` = **111** (a value absent from the whole corpus) on the fixture at DMX 81, whose three identifiers disagree: display position 4, `slot_id` **0**, address 80. Three rivals are pre-registered **as exact byte arrays** — `[20, 129, 127, 127, 127, 110]`, `[16, 129, …, 114]` and `[129, 127, 127, 127, 127, 3]`, the last being byte-identical to COV-27's, which is its own tell — plus six predictions on what must not move, record 120's 111 entries included. A **stage 2 reorder** splits the pair the identity display order leaves degenerate, and its outcome A does not close on the file: the discriminator is firing the macro and watching which head moves, which would also be the first direct evidence for the stale-macro reading COV-56 offered for its own failures. **A free rider on record 161**: this project has never had a macro, so stage 1 is the cleanest test there will be of whether item 1's 20-bit head is a Live Edit **button** mask — it is `[0, 0, 0]` here on a project whose preset has `LIVE EDIT` **ON**, which already refutes the enable reading from the corpus alone. | planned | §5.5 |
| COV-59 | 2026-09-01 | **552 of record 161's 564 values are zero in every file, and calling them `partial` was false in the walker's own words.** `partial` is defined as *it varies, and nothing reads it*; a positional sweep of the three arrays over the corpus shows only **nine** positions ever differ — item 0's 0, 1, 2 (COV-45's fixture mask, and it is three values wide, not one), item 1's 0, 1, 2 (COV-21's 20-bit mask) and index **187** on all three items (the shared tail) — and the remaining 555 carry 0 in all 104 files. So inertia is now declared **per position** for one packed array, positions 3–186, under the same contract every other inert entry has: the value is asserted on every file, the widths are **measured** rather than assumed, and a second value raises `InertBroken` and stops the run — verified by declaring position 2 inert on purpose and watching it fire on the 7 it carries. **Nothing is named and Q8 is untouched.** What changes is the size of the question: the corpus reads **93.3 % read, 6.0 % inert, 0.7 % partial**, and record 161's share of `partial` falls from 2.05 % to **0.09 %** — of which the values that actually move are **0.05 %**. The 1.96 % that moved buckets is a statement about the corpus and it is written as one; it is also the statement the ledger has been making in prose since COV-21 (*the other 182 values of every item have never moved in 82 files, 13 projects and two authors*) without the counter reflecting it. **This is the opposite of emptying the inert bucket by renaming it**, which is the move that would reach 100 % by regressing: nothing gained a name, `read` did not move by a byte, and the claim became falsifiable by the next file instead of staying an assertion in a paragraph. | correlated | §3 |
| COV-58 | 2026-09-01 | **`115.f6`/`f7` swept across the nine device saves already at the repository — a negative, and it kills four hypotheses at once.** The pair had never been looked at: the ledger carried six scattered observations (COV-32, COV-13, COV-40, COV-42, COV-44, COV-46, COV-47, COV-52, COV-53) and no analysis. Read by project and in version order, **25 files carry it and 67 of 86 versions carry nothing at all** — absence is the norm, not the exception. (1) **Not per-fixture data**, from a second direction: every row of a file carries the *same* two values, in all 25, so a per-fixture attribute it is not — COV-42 said this from the fact that a save deletes them. (2) **Not a checksum of record 115 or of the patch**: `(231, 20)` sits on two record-115 payloads that differ, and on **two unrelated projects** — the twenty-fixture experiment rig and the four-fixture project the panel created from nothing, six days apart. (3) **Not a monotone counter or a watermark**: read as a little-endian 16-bit value the experiment rig runs 10540 → 4 → 29160 → 5351 → 18 → **29160** → 1593 → **29160** → 5351, so it **returns to a value it left**, twice, across days and dozens of saves. (4) **Not a fragment of the save time**: a 16-bit slice of a clock does not land on 29160 three times in a week, and the container's own version counter reproduces none of it under any of `mod 256`, `mod 65536` or `//1000 mod 65536`. **And the presence does not follow the operation**: on the clean project it is written after create, delete, reorder, reorder and two adds, and absent after a detach and two other adds — the same operation on both sides. What is left is a transient global copied into every row when it is non-zero, and nothing here names it. **The decisive test is already planned and unrun**: COV-38's two no-op saves. If the pair moves — appears, changes or vanishes — between two saves that change nothing else, it is not project data, and that is a publishable result rather than another form. COV-38's sheet now has a second deliverable and needs no extra manipulation to get it. | observed | §7 |
| COV-57 | 2026-09-01 | **`120.f5` and `f6` are the 16-bit pairing**, and the reading arrived by printing them together — which is what COV-50 says it got wrong the first time, on the same two fields, from a print that dropped them. `f6` is the absolute DMX channel of the **other half** of the entry's 16-bit pair and `f5` says which half this entry is, **1** principal (coarse) and **2** fine. Three things, each exact on every clean file: the relation is an **involution** — the partner points back, carrying the opposite half — **2744 of 2849** paired entries, and all 105 failures are the five captures of the COV-53 defect; `f5` follows the **direction** with no exception anywhere, 1 whenever the partner sits above and 2 whenever it sits below, the only 20 departures being two of those same captures; and cross-record, on the fine half `f6 − dmx_address` equals **`110.principal_channel`**, **1392 of 1392**, absent read as 0. That last one is the reading's own explanation of why nobody had it: COV-03 measured `f6` against `110.principal_channel` and got **954 of 2081**, *enough to notice, nowhere near enough to name* — only the **fine** halves match, and the coarse halves point the other way. It also settles the intra-block order **COV-50 explicitly declined to read**: a head at address 16 carrying 18, 19, 16, 17 is Pan and Tilt pointing at their fine halves, then the fine halves pointing back. **COV-17's negative is not overturned, it is explained** — its `k XOR 1` partner hypothesis failed at 131/2157 because the partner is a **DMX channel** and not an index into 120, and its coarse/fine test compared `f5` against the *presence* of `110.f5` rather than against the partner relation. **The status is `correlated` and the denominator is the reason**: the 2849 entries are **three** distinct block patterns replicated, and what makes them worth something is that the two topologies disagree — a 16-channel head pairs 0↔2 and 1↔3, an 8- or 14-channel fixture pairs 0↔1 and 2↔3 — so a rule fitted to one fails the other. A fourth profile with a pair at a new offset, predicted before the save, is what raises it. **A status went down and it should have**: those bytes were named `dmx_channel` at device-confirmed this morning on COV-50's weaker claim; the sharper name `partner_channel` claims the pairing, which is correlated, so the coverage figure loses 0.21 % of device-confirmed and gains a name that says what the field is. `_anomalie_120_alignement` now checks the pairing as well as the span, and sees the same five captures from the other side. | correlated | §7 |
| COV-56 | 2026-09-01 | **`160.f7` is the same run-length map as `120.f4`, and COV-28's refutation was looking for a value where the bytes hold a position.** Found by pattern rather than by measurement: COV-28 recorded the diff as *a leading `129 → 4` and a trailing `3 → 126`*, and under COV-55's rule — under 128 a free run, at or above 128 an occupied run of `v − 128` — those two arrays are one occupied slot at **0** and one at **4**, both maps covering exactly 512 slots. Swept: on all **594** corpus macros the runs sum to **512**, the occupied slots number exactly `len(f1)` (COV-20's one mask per targeted fixture), and re-encoding the decoded map reproduces the stored bytes **byte for byte**. The factory macros fall out too: *eight `(x, 129)` pairs and a four-varint tail* is eight isolated occupied slots. A structural corroboration rides along — bit `2k` of an `f1` mask is used and bit `2k+1` **never**, 0 of 594, which is the stride COV-27 measured when `01 Pan` gave 17 and `02 Tilt` gave 68 = 17 ≪ 2. **What a slot indexes is NOT read and the field keeps its neutral key.** Two candidates are recorded rather than chosen: slot *p* = DMX channels `[4p, 4p+4)` resolves **2998 of 3470** targeted bits, and slot *p* = a sixteen-channel window at display position `p // 4` holds on **12 of 30 distinct** macros — and counting distinct is what the corpus rule demands, because the misses are five macros replicated across seventy-seven files. Both fail on the same entries, the non-moving-head ones, which is consistent with a macro left stale by a re-patch — the accumulation COV-26 measured on record 125 — and consistent is not measured. 86 % is exactly what COV-17 refused to name `120.f5` on. `cartes_de_macro_160` checks the 512 and the slot count on every file; the field gains a wire **kind** and no name, the move `161.items.f3` got. **The settling experiment is one macro**: on a project the panel wrote from nothing, one fixture at a known address, then the same fixture moved — the first reading predicts the slot, the second predicts it does not move. | correlated | §5.5 |
| COV-55 | 2026-09-01 | **`120.f4` is the DMX occupancy map**, and it is the third field the unnamed-field sweep found already answered by captures on disk. COV-29 had the only attribution it ever carried — *the one thing an address move writes*, its 13 packed varints reordering their tail — and never opened them. Read as **run-length**, one varint per run in channel order, under 128 a free run and at or above 128 an occupied run of `v − 128`: four heads at 0/16/32/48 give `[192, 127, 127, 127, 67]`, 64 occupied then 448 free split at the 127 a run caps at. Three checks, each on **104 of 104** files: the runs sum to exactly **512**, the occupied channels number exactly as many as record 120 has entries, and re-encoding the decoded map reproduces the stored bytes **byte for byte**, the firmware's own split points included — so the reading explains every byte of the field and not merely its shape. **The single-variable differential is COV-29's own pair**, re-read: moving one fixture 500 → 300 takes channel **499** out of the map and puts **299** in, one channel, base 0, and the return to 500 restores the array identically. **It maps the record, not the patch**, which is what makes it worth more than a decode: on the eight files carrying COV-32's surplus of six the map marks six channels the patch does not justify — **237–242** — so those six entries are a real ghost block and not a stale counter, which COV-32 could not say; and on the four captures of the COV-53 defect the map is short by one, the same entry the record lost. `occupation_120` now checks the 512 and the count on every file. **Not device-confirmed, and the reason is the method, not the evidence**: this is a reading taken *after* the captures were opened, with no prediction published before one, and nothing written under it has been accepted by a W1. The cheap promotion is a free rider — predict the exact `f4` bytes of the next device save before downloading it. | validated | §7 |
| COV-54 | 2026-09-01 | **The unnamed-field sweep, run against the ledger instead of against the bytes** — the third time the coverage counter has returned unpromoted work rather than unknown work (COV-01 gave back 25 points, COV-50 gave back one field and it stayed `partial` for a day). Every path the walker calls `partial` was matched to what `research/evidence.md` already establishes about it, and every name in `PROOF` to the strongest entry that supports it. **Three fields were named**: `120.f6` → `dmx_channel` (COV-50, device-confirmed, re-verified at **2731 of 2762** corpus entries inside their own fixture's span, all 31 exceptions belonging to captures of the COV-53 defect); `115.f9` → `slot_id` (COV-46/47/49/52/53, device-confirmed, distinct within a file on the **92** files that carry it); and `120.f4` → `occupancy`, which is COV-55 and is a new reading rather than a promotion. **Four statuses rose** on names that were already there and were being credited to a weaker source than the ledger holds: `110.feature` correlated → device-confirmed (COV-25, the `FIXTURE BUILDER` screen, 16 of 16), `115.display_order` → device-confirmed (COV-47, the reorder the operator read back on the panel), `106.dmx_channel` → device-confirmed (COV-29, the single-variable address move), `125.name` → device-confirmed (COV-08, the panel's own header). Corpus: **92.7 → 93.1 % read**, 3.2 → 2.9 % partial, and the split that matters, **54.9 → 56.6 % device-confirmed**. **What was checked and deliberately left `partial`**, because the sweep is only honest if it says what it refused: `160.f7` — COV-27/28 device-confirm that it *encodes the targeted fixture* and refute all three candidate encodings, so a name would claim a reading of bytes nobody has read, which is renaming to empty the bucket; `120.f5` (COV-17, three hypotheses refuted); `115.f6`/`f7` (a pair saves add and remove, never read); `125.f4_tail`, `161.items.f1`/`f3`/`f4`, `165.f1`, `116.f6`, `102.f11`. | correlated | §3, §7 |
| REPAIR-01 | 2026-09-01 | **Not a measurement — a repair**, recorded for provenance like any other artefact. The experiment project's position layer, fixed by [`corpus/experiments/REPAIR-01/build.py`](../corpus/experiments/REPAIR-01/build.py): record 151 to its empty form, record 150's three group-A slices removed, `120.f1` from 221 to the 215 entries the record holds. **3 anomalies → 0, 24/24 identities**, and only records 120, 150 and 151 differ — the prefix and the other seventeen records, record 165 included, are byte-identical. **The surviving detached entry was dropped rather than repointed, and the file is what decided it**: it names fixture 2, a `6x18W 6in1 RGBAW UV` whose ten channels carry neither pan (1) nor tilt (2), so it stores pan and tilt offsets for a fixture that cannot pan or tilt. Repointing it would invent an intention nobody recorded, and the five entries COV-23/24 dropped are unrecoverable. What the repair writes is what the **panel already shows** — no detached fixture (COV-33) — so the file is made to agree with the device rather than the reverse. **Not deployed.** It is a file on disk; writing it to the controller needs the operator's word, and the honest caveat is that COV-37's regression was never explained, so a later save could undo it. sha256 `3a3687cdff35208c…`. | deliverable | §8.1 |
| REPORT-01 | 2026-09-01 | **Not a measurement — a deliverable**, recorded here so it has provenance like anything else this repository publishes. A bug report for the manufacturer, [`docs/report-record-120.md`](../docs/report-record-120.md), on the defect COV-53 characterised: adding a fixture to a group other than the first drops one entry from the head of record 120. It rests on COV-53 for the measurement, COV-50 for why a count check misses it, and COV-16 for what the record is. Written with **no serial, no project identifier and no name from this rig**, and it says up front that "record 120" and "field 6" are our labels, giving the vendor a way to map them: the record whose entry count is the sum over patched fixtures of their profile's channel count. Its scope section is deliberately narrow — one unit, one profile, **groups A and B only**, and no claim about the cause. The three uncharacterised observations travel in their own section, labelled unverified. The minimal pair backing it is `corpus/experiments/REPORT-01/`, one save apart and one change between them, the before **structurally clean at 0 anomalies**: `4064e36f859a4718…` → `4db2b8a384922837…`, 64 entries of a required 64 against **79** of a required 80. | deliverable | §7 |
| COV-53 | 2026-09-01 | COV-49 stage 3 — the last ambiguity in the trigger, planned and published before the measurement. Sheet: [`corpus/experiments/COV-49/stage3.md`](../corpus/experiments/COV-49/stage3.md). Stage 2 ruled out the display order; what is left is whether the defect is *adding to a group other than the first* or *adding to a project that has more than one group*, since rung 1's clean add to group A was on a one-group project. A seventh head added to **group A** separates them. **The anomaly count cannot do it** — the file is already one entry short and already misaligned, so two anomalies stand either way — and the sheet says so: the discriminator is the exact size of record 120, **111** for a clean add against **110** for another drop, with 112 pre-registered as the unlikely repair. Six further predictions on records that should not move, including COV-45's fifth test at mask **63 → 127**. Either answer completes a bug report reproducible in five minutes from a project the device wrote from nothing. **Outcome G, and the defect is characterised.** Record 120 came back with **111** entries — 95 + 16, the deficit unchanged at one — so the add to group A was **clean on a two-group project**. The trigger is **adding a fixture to a group other than the first**, not the number of groups: two adds to group A gave +16 each (COV-44 rung 1 and this one) and two adds to group B gave **+15** each (stages 0 and 2). Two for two, both ways. **The symptom is precise and cumulative**: it is the entry at the **head** of the record that goes. Reading the first `f6` against the lowest DMX address, the table starts one entry late after the first group-B add and **two** after the second, and stage 3's add to group A moved it by none. The arithmetic closes exactly — `112 required − 2 dropped + 1 appended by the stage-1 save = 111`. So every fixture carries its neighbour's channel data, one channel out of step, and a later save can restore the **count** by appending at the tail while leaving the shift (COV-50) — which is why a counter check cannot see it and `120.f6` had to be read first. **Not observed on** adds to the first group, deletions, reorders, address changes, or detach-with-offset: nine device saves, and only the two group-B adds do it. All six side predictions held — `f9` = **6**, `display_order` `00010203040506`, record 151 and `Floor` untouched, record 125's group-A slot unchanged and now decoding as `{profile_mask 1, f4_tail 184}` under COV-51's varint, `115.f6` a seventh distinct form at **5**, and **COV-45 a fifth time** with the item-0 mask **63 → 127**. sha256 `ad388a41d63ca45a…`. | device-confirmed | §7 |
| COV-52 | 2026-09-01 | COV-49 stage 2 — **outcome A: the second group is the trigger, not the display order.** Sheet committed before the capture was opened, though the operator had already run the change; the prediction was stated in session first and the sheet records that as the weaker form. A sixth `Lyre ZQ02244` added at DMX 97 in **group B**, on the **identity** `display_order` stage 1 restored → record 120 came back with **95** entries against the **96** the patch justifies. **It dropped one again**, with the display order out of the picture. Adding to group A was clean (COV-44 rung 1); adding to group B has now broken it **twice**. Two readings still fit and the difference matters: it may be *adding to a group other than the first*, or simply *adding to a project that has more than one group* — separated by one more save, a fixture added to **group A** now that a second group exists. Four predictions held: `f9` = **5**, the lowest free number after 0-4; `display_order` `000102030405`; record 151 and `Floor` untouched; and record 125's group-B slot unchanged. **COV-45 confirmed a fourth time**: the item-0 mask **31 → 63**, all-ones over five fixtures then six. `115.f6`/`f7` took a sixth distinct form, **18** with `f7` absent. sha256 `66ddf4577e03376e…`. | device-confirmed | §7 |
| COV-51 | 2026-09-01 | **`125.f4`'s tail is a varint, not the fixed byte the split assumed.** It read `0x30`/`0x38` on **891** corpus slots and is **two bytes** on 18 — the nine slots of the two most recent captures — where it reads `b8 01`. Varint or little-endian is decidable here and not a preference: a fixed-width field does not change length, `0xb8` carries its continuation bit set which *requires* a following byte, and 56 → **184** is one value crossing 128. Little-endian would read 440 and would not explain the widening at all. So the A3 split of 2026-09-01, `(6 bytes, 1 byte)`, was wrong; the schema now reads `(6 bytes, varint)` and the coverage walker **measures** a varint part rather than assuming its width. **The guard is what caught it**: `_split` returns `{"hex": …}` when the pieces do not consume the payload exactly, so the codec round-tripped the wide form verbatim instead of decoding it wrongly — the fallback written with the split, doing the job it was written for. This supersedes COV-22's *"the 7th byte is `0x30` or `0x38`"*, which was true while the value stayed under 128. **Which save widened it: the reorder** of COV-50's stage 1, where record 125 is in the changed list and I did not open it — recorded as the miss it was. | device-confirmed | §7.2 |
| COV-50 | 2026-09-01 | **T3 held — the count was repaired and the alignment was not — and `120.f6` is an absolute DMX channel.** Restoring the identity display order took record 120 back to **80** entries with `f1` following, and `_anomalie_120_patch` cleared. It did it by **appending one entry at the end**: `stage0 == stage1[:-1]`, exactly. The missing entry was at the **head**, so every fixture's block still begins one channel late and the record is a channel out of step from index 0. **A counter is not an alignment**, and the per-file check said 0 anomalies on it — my own instrument, blind to the thing it was built for. **First I got this wrong.** The reading rested on a print that showed only each entry's `value` and dropped `f4`/`f5`/`f6`; under it the blocks looked identical per profile, and a rule built on that failed on 169 groups including clean files. Printed in full it is `f6` that tells the story: a fixture at address 16 carries `f6` **18, 19, 16, 17** and the next, at 32, carries **34, 35, 32, 33**. So **`120.f6` is an absolute DMX channel inside its own fixture's span** — **2671 entries of 2679** across the corpus, and the **8** that are not belong to these two captures. That is a reading of a field COV-17 had refuted three hypotheses on, and it is now the **fifth anomaly**, the one that sees a misalignment a count cannot. The trace: stage 0 shows **2**, stage 1 shows **1** — the count repaired, the alignment still broken. sha256 `cbc71cd4612249de…`. | device-confirmed | §7 |
| COV-49 | 2026-09-01 | Whether a **session** reproduces what no single operation did, planned and published before the measurement. Sheet: [`corpus/experiments/COV-49/prediction.md`](../corpus/experiments/COV-49/prediction.md). It opens by cutting down its own predecessors: five device saves on the fresh project came back clean, but COV-48's anomaly is *an entry naming a fixture of another group* and that project has **one** group — the anomaly was **unexpressible** there, so those five could not have found it whatever the writer did. Stage 0 therefore adds a fixture in **group B** to make it expressible, and is itself a prediction: if that alone dirties the file, the answer is simpler than a session. Stage 1 is the session COV-23/24 described in its own words — *moved through the setup screens and saved once at the end* — as three index-shifting operations before **one** save, where every rung so far saved after one change. The firmware demonstrably remaps `151.f1` across a single deletion (COV-46); the untested case is two shifts before one save. Four mutually exclusive futures for the entry are pre-registered, of which two — it survives naming a group-B fixture, or under a count 151 cannot serve — are the halves of the format-lab's damage. If both stages come back clean, no operation and no session reproduces it and **WTOOLS is the last suspect standing**, which is COV-38. **Stage 0 measured, and it refutes its own S0-4: one save, one anomaly, on a project written entirely by the device.** Adding a fifth fixture left record 120 with **79** entries where the patch justifies **80**, and `120.f1` follows the content at 79 — so the counter is right and the *content* is short, the mirror image of the format-lab's surplus of six. The shape is worse than the count: laid out in blocks of sixteen, every block reads `127 None None . . 241 220 7 …` where the clean state read `127 **127** None None . . 241 220 7 …`. **The first entry is gone and the whole table has shifted left by one**, so each fixture now carries the values of the next channel and the last block ends fifteen long. **The single-variable claim does not hold and it is mine to own:** rung 1 added a fifth fixture cleanly, but to **group A** on a project whose `display_order` was the identity, and this one adds to **group B** on the non-identity order rung 3 left behind. Two candidates, and a mechanism worth testing over guessing — record 120 is in ascending **DMX address** order (COV-16), so a writer that walks the *display* order to emit it would drop exactly one block boundary. Held: S0-2, group B's slot gained `profile_mask` 1; S0-3, record 151 and `Floor` untouched; and COV-45 again, the item-0 mask **15 → 31** for four fixtures then five. **S0-1 is half refuted and the half is a reading**: the new row's `f9` is **absent = 0**, not 5 — it reused the number the deleted fixture freed, so `f9` is a reusable slot identifier and not a monotone counter. Record 145 also moved, group B's palettes being populated. sha256 `4db2b8a384922837…`. | device-confirmed | §7 |
| COV-48 | 2026-09-01 | **The format-lab's corruption is named, and it is checkable from the file alone.** COV-47 settled that `151.f1` indexes record 115, which is what made this definable: record 150 has one copy per group, so a slice of 151 belongs to the group whose copy cuts it, and the fixture it names must be in that group. Swept over the corpus: **353 entries consistent, 11 pointing outside their group — and all 11 are the format-lab**, from the COV-23/24 session onwards. The surviving detached entry names a fixture of **group 1** on a slice owned by **group A**, which is why the panel surfaces no detached fixture at all (COV-33) and why nothing caught it: the index is in range, so `tranches_151` passes. The reference also moved from fixture 0 to fixture 2 at COV-32, the same save that reverted 150's slice table — wrong before, wrong differently after. **This corrects the rung-4 sheet**, which said an offset landing on the wrong fixture had "no file-internal way to define it, which is itself the finding". It has one; the finding was that the index space was ambiguous, and a reorder resolved it. Now the fourth anomaly of the per-file check. The forensic trace it gives is clean: **0** anomalies before the session, **2** after it, **3** after COV-32's save — and **0** across all five device saves on the fresh project, covering create, detach, add, delete and reorder. **No single patch operation reproduces the damage.** | device-confirmed | §8.1 |
| COV-47 | 2026-09-01 | COV-44 rung 3 — reordering the fixtures, **planned and published before the measurement**. Sheet: [`corpus/experiments/COV-44/rung3.md`](../corpus/experiments/COV-44/rung3.md). Rungs 1 and 4 came back clean, so reordering is what is left of "the patch was reorganised", and it is the operation COV-26 caught making record 125's profile mask accumulate. **This project cannot test that half** — it has one profile, so profile reordering is impossible — and the sheet says so. What it can test is worth more than its ladder position: `151.f1` is a fixture index, and `display_order` has been the **identity permutation** on every capture of this project, so "rank in display order" and "index into record 115" have been indistinguishable throughout and COV-33's D3 could not separate them. **Reordering breaks the degeneracy in one move.** Four mutually exclusive outcomes are pre-registered with what each one reads, plus seven predictions on the records that should not move. The one the ladder exists for is **D**, the offset ending up on a different head — which `wpj_identities` would report as **0 anomalies** while being wrong, an index in range pointing at the wrong fixture, exactly the shape rung 4's R1 would have had. If this rung is clean too, no single patch operation on fresh data reproduces the damage and the suspects left are WTOOLS, which COV-38 would name, or something particular to that project rather than to any operation. **Measured: outcome A, and every prediction held.** `display_order` went `00010203` → **`01020300`** while record 115's rows kept their order and their addresses, and **`151.f1` stayed 0** — so **`151.f1` is an index into record 115, not a rank in display order**, and COV-33's D3 is answered. **Device-confirmed at the panel:** the operator reports the head that was `Lyre Z 1` is now `Lyre Z 4` and still carries the 40 %, so the offset followed the physical fixture rather than the slot. Only **105 and 115** changed; 106, 110, 111, 116, 120, 150, 151 and 161 are byte-identical, and P1 held — record 120 is in ascending **DMX address** order (COV-16) and a display reorder moves no address. Record **105's items follow the display order**: its offsets came back `[10, 20, 30, 0]` against fixture refs `[1, 2, 3, 0]`, which is new. `115.f9` kept 1, 2, 3, 4 with their own addresses, and `115.f6` went **231 → 3** while `f7` stayed 20 — a fourth kind of operation writing that pair differently, and the first time the two have moved apart. **0 anomalies.** sha256 `4064e36f859a4718…`. | device-confirmed | §7, §8.1 |
| COV-46 | 2026-09-01 | COV-44 rung 4 — **deleting a fixture is clean, and the firmware remaps the reference**. The first fixture deleted, index 0, *not* the one carrying the offset, so that every index above it slides. Sheet committed before the capture was opened; the prediction had been stated to the operator in writing before they measured but was not in the repository, which is weaker and the sheet says so. **All eight predictions held and all three rivals fell.** `151.f1` was rewritten **1 → 0**, emitted as *absent* — and absent is 0 here exactly as it is for `115.dmx_address` (COV-41), so a reader must not take it for "no fixture". The entry is otherwise byte-identical at `pan_offset` 45874 and `Floor` still claims 1. **R1 is refuted, and it was the dangerous one**: had the reference stayed at 1 the stored offset would silently address the wrong head, with 0 anomalies, because the index would still be in range. Three more readings ride along. **COV-45 holds on a shrink**: record 161's item-0 mask went **31 → 15**, all-ones over five fixtures then over four, after being measured on a growth. **`115.f9` keeps 1, 2, 3, 4 on the survivors** — a deletion does not renumber it, so it is a stable per-fixture identifier and COV-15's refutation of the position-index reading is extended rather than merely repeated. And **`115.f6`/`f7` came back as 231/20** after being absent through the add — a pair the *format-lab* also carries, on 20 fixtures where this project has 4, so the pair is **not** a project identifier and COV-38's collision check said otherwise and is corrected. What it is remains unread; that a **deletion** writes it, where an add did not and a plain save deleted it, is the sharpest thing said about those two fields yet. **The hunt narrows:** adding and deleting are both clean on fresh data, so neither caused the format-lab's 150/151 damage. sha256 `5395eea127b616e1…`. | device-confirmed | §7, §8.1 |
| COV-45 | 2026-09-01 | **Record 161's leading value is a bit mask over the fixtures** — the first attribution anything in that record's *content* has ever had, and it came out of COV-44's rung 1 for free. Read the leading packed values of an item as the little-endian bytes of one integer: on the single-variable save that added a fifth fixture, item 0 went from **15** to **31** — all-ones over four fixtures, then over five. Swept over the corpus, **the highest set bit never addresses a fixture that is not in record 115: 288 items, no exception**, and that is now the 21st identity. It settles what COV-21 could not: the "20-bit mask" it found is not a palette page, a preset page or a Live Edit page — all of which are also 20 — because those files carry **20 fixtures**. Corroborated by *rig-c-bug*, which is *rig-c* re-patched from 20 fixtures to 22 and still carries the 20-bit all-ones. **What the mask selects is not read**: the format-lab holds 8 bits of 20 in one version and 2 in another, so it is a selection and not a constant, and a per-fixture UI state is a guess and is written as one. Q8 stays open on the other 187 values. The byte reading is **guarded, not assumed** — a value above 255 means the array is not a byte string there and the item is skipped, which is the trap record 155 sprang. sha256 of the rung-1 capture `6cd7df554b4b6b68…`. | observed | §11 |
| COV-44 | 2026-09-01 | Whether the damage **reproduces on clean data**, planned and published before the measurement. COV-42 gave the negative that makes it worth asking — the 150/151 layer writes correctly on a fresh project — so the trigger is a **patch edit**, which is what the COV-23/24 session was. A ladder, one change per save, stopping at the first rung that reports an anomaly: add a fixture, move an address, reorder, delete. Sheet: [`corpus/experiments/COV-44/prediction.md`](../corpus/experiments/COV-44/prediction.md). Rung 1 is fully numbered from the current state, eleven predictions including two rivals that are each the negation of one number — the record-151 entry surviving, and `120.f1` following its entry count, which is COV-32's anomaly. A free one rides along: `115.f6`/`f7` are **absent from every row** right now after COV-42 watched a save delete them, so their return on a save that touches the patch would read them as written when the fixture list is edited. The bet, stated in advance: if rung 1 is clean the trigger is rung **4**, because `151.f1` is a fixture index and deleting is the only operation that moves every index above it. **Rung 1 measured: adding a fixture is clean.** Eight of nine predictions held and both rivals fell. Five records moved — 105, 106, 115, 120, 161 — and no others; version 2 → 3, 9823 → 10050 bytes. 115 gained the row at address **64** with `f9` **4** and `display_order` `0001020304`; 105 gained `offset_106` **40**, `fixture` **4**, `entry_count_106` **10**; 106 went to **50** with its `f1` following; 110, 111 and 116 were untouched, `channel_names` included. **P6 held and R2 fell**: record 120 went to **80** entries and `120.f1` followed to 80, so COV-32's stale counter does **not** reproduce on a fixture add. **P7 held and R1 fell**: record 151 came back byte-identical — `fixture` 1, `pan_offset` 45874 — and `Floor` still claims 1. **0 anomalies.** The one refutation is **P9**: `115.f6`/`f7` did **not** come back, absent on all five rows including the new one, so they are not written when the fixture list is edited either. Created with them, first save deleted them (COV-42), and a patch edit does not bring them back. The trigger is further up the ladder, and the bet published with the sheet stands: rung **4**, deleting a fixture, because `151.f1` is an index. | rung 1 of 4 measured, clean | §7 |
| COV-43 | 2026-09-01 | **`116.f12` is the profile's per-channel names, and COV-30's reading is confirmed by the cleanest possible file.** The project the panel created from scratch carries a single `Lyre ZQ02244` profile with **16** `f12` entries for its 16 channels, aligned to its own `110.f4` types: `Pan ` and `Tilt ` on the two fine channels, `Pan/Tilt Speed` on Speed, `Strobe` on Shutter, and **`Color Effect` and `Reset` on the two `Generic`** — name for name what the `FIXTURE BUILDER` screen printed in COV-25, and index for index the six names COV-30 reconstructed out of three other profiles' items. One profile means no mis-slicing is possible, which is what makes this the confirming case. So the field is **named** `channel_names`, at `device-confirmed`, and it leaves the `partial` bucket. **COV-30 rises from `observed` to confirmed** and its warning stands unchanged and now proven: WTOOLS 2.0.2 cut one profile-local array with each item's *global* offset into record 110, so in those eight files the same sixteen names are spread across three other profiles and the profile they belong to carries none. A reader must not trust the alignment blindly. | device-confirmed | §7.6 |
| COV-42 | 2026-09-01 | **The 150/151 layer round-trips correctly on clean data**, which is the negative that narrows the whole hunt. On COV-40's fresh four-head project, one fixture detached on the factory-named `Floor` pad with `PAN OFFSET` set to **40 %** and nothing else, then saved. Prediction published in the same breath as the comparison and it held: record 151 gains **one** entry with `pan_offset` = 45875 ± 1 — **stored 45874**, `+39.998 %` — and record 150's `Floor` slot gains `entry_count_151 = 1` with no offset field, which is 0. `wpj_identities`: **20/20, 0 anomalies**. Three records moved and no others: 115, 150 (+2 bytes) and 151 (0 → 18). So the writer is **not** permanently broken on this path, and the format-lab damage belongs to that project or that session rather than to the feature. Two readings come free. **`151.f1` is the fixture and the panel numbers it from 1**: the screen showed `Lyre Z 2` and the file stores `fixture = 1`. It does **not** discriminate display rank from record-115 index — this project's `display_order` is the identity permutation, so both give 1, and COV-33's D3 stays open. **Neutral is 32767, not 32768**: the untouched tilt and focus offsets store 32767 and the screen prints `0 %`, so a writer emitting 32768 would be writing something the device never writes. And the surprise: **`115.f6`/`f7` were deleted by the save** — all four rows lost `177`/`124`, record 115 shrinking 64 → 44 bytes. COV-13 and COV-32 watched saves *add* that pair; this one removes it on a save that touched nothing about the patch. A field that evaporates is not per-fixture data, and that is the first thing anyone has been able to say about those two. sha256 `806fef9a1f617115…`. | device-confirmed | §7, §8.1 |
| COV-40 | 2026-09-01 | A **clean reference**, created on the panel rather than synthesised, because a tool-authored project would add the one variable a writer-bug hunt has to remove — *is the file wrong or is the firmware wrong*. Four `Lyre ZQ02244` at addresses 0, 16, 32, 48 in group A, one preset, 9823 bytes, version **1**. `wpj_identities` on it: **20/20 identities, 0 anomalies**. Records 150 carry no slice of 151 and record 151 is **empty**, so a fresh project starts with no detached fixture and the whole `150`/`151` layer is at its origin. `115.f6`/`f7` are **177/124** on all four rows — uniform within the file again, a seventeenth file and a pair no other project carries. sha256 `1060d857571bf2ab…`. This is the baseline every following measurement is differenced against. | device-confirmed | §7 |
| COV-39 | 2026-09-01 | **Turning the `PAN OFFSET` / `TILT OFFSET` encoders on the `Fixture Offset` page crashes the controller**, reported by the operator during COV-33's encoder turn. Recorded as it stands and no further: what the crash looked like, whether the unit recovered on its own, whether a project was dirty at the time and what state it came back in are **not established here**. It matters beyond a nuisance for two reasons. It is the fifth defect on the same screen and the same records — after the misplaced `116.f12` (COV-30), record 120's surplus, five detached fixtures lost in one editing session, and `150`'s slice regression (COV-37) — which is what makes the operator's reading, that this is a **writer bug and not a subtlety of the format**, the one the evidence supports. And it is a **hazard for every experiment on this screen**: a crash during a save is how a project gets truncated, and this one has already lost show data that way. No sheet should send anyone near those two encoders until it is characterised. | observed | §8.1 |
| COV-38 | 2026-09-01 | Which **writer** made the save COV-37 caught, planned and published before the measurement. The version counter increments the same for the panel and for WTOOLS, and nothing this repository reads carries an author; COV-02 found one split on a save that *changed* something, and nobody has asked what each writer touches on a save that changes **nothing**. Two no-op saves, one per author, and a diff. Sheet: [`corpus/experiments/COV-38/prediction.md`](../corpus/experiments/COV-38/prediction.md). **Revised 2026-09-01 for a second attempt, and the revision is the point**: the first attempt's two files are **byte-identical, version counter included**, so *the panel save never happened* and *it happened and wrote nothing* are indistinguishable — a filename is not a measurement. The method now reads the version **off the device** with `projects`, which returns it without a download, before and after each save. Fifteen predictions. The sharp one is **P8**: COV-02 measured that WTOOLS writes `165.f27` = 1000 where the panel writes 0 but never asked at which *moment*, and this project's six presets all carry `[0] × 8`, so one no-op save says whether a single field names the author of any save — retroactively, over the whole corpus — or whether COV-02's split is about how a project is born. **P10 is COV-58's decisive test** riding along: if `115.f6`/`f7` moves across saves that change nothing else, it is not project data. Five free riders check this session's readings at no cost, COV-55's occupancy count and COV-57's pairing among them. **Ordering, and it is why this is urgent:** restoring the project destroys the only live instance of the corrupted state, and this is the only planned experiment that needs it. **Step 1 attempted 2026-09-01 and it did not execute** — the counter did not move, `projectChanged` was false, the download was byte-identical, no project on the controller moved, and the panel displayed nothing. COV-63 is what came out of chasing that instead, and it settles the method: an executed save increments the counter, so this sheet's oracle works and the non-event was a non-event. The panel half is **still unrun as designed**. **The WTOOLS half is unrun too, and the attempt is worth recording as a warning rather than a result.** What was performed on 2026-09-01 was not a no-op save: the operator saved to the cloud, **stored the project onto the controller**, then opened it. The capture shows all **twenty records byte-identical**, the version counter **unmoved**, and the project's **UUID replaced** — the derived identifier gone from the controller and a WTOOLS-side one in its place, the container SHA-1 recomputing over it. A store replacing a project entry is unremarkable; *a save replacing an identity* would not be, and nothing here measured a save. The entry is written so that the byte-level fact survives and the reading it invites does not. The operational consequence is real though: the managed write path targets the derived UUID, so `deploy` refuses until the experiment is re-initialised. | planned | §10 |
| COV-37 | 2026-09-01 | Records 150 and 151 read **by version** across the experiment project's 73 files with a record 151, which settles COV-32's open side and corrects what COV-33 was told. The history: at versions `…320/…321` the record holds **6** entries and 150 claims 6, slices `A1:0+4 A4:5+1 A5:4+1`. At `…322`, the `FIXTURE SETUP` session of COV-23/24, it falls to **1** — and **150 follows correctly to `A1:0+1`**. It stays 1/1 for five versions. Then at `…327`, COV-32's unattributed save, 151 is still 1 and 150's slice table is back to `A1:0+4 A4:5+1 A5:4+1`, **byte for byte the table from before that session**. So the fall of `tranches_151` is a **regression, not a stale counter**: **150's side is the stale one**, settled from the corpus with no hardware, and COV-32's "which side is stale is not settled" is superseded. **Two of my own statements are corrected by this.** "Record 151 was truncated on write by that save" is wrong in direction — the truncation happened five versions earlier and was *consistent*. And what is true, and worse, is the loss itself: **the COV-23/24 session dropped five of the operator's six detached fixtures**, which is the data loss that reading was groping for, in the wrong place. **COV-33's design fault is also closed**, by measurement rather than by argument: `GET_SETTINGS` reports `projectChanged: false`, so the panel was showing the stored file and the comparison was valid. The panel shows **no** detached fixture at all — not even the one entry the file still holds — which reads as the firmware refusing a slice table that outruns its record rather than surfacing what it can. That last step is a **reading, not a proof**, and it is cheap to test: version `…326` carries the same single entry under a consistent `A1:0+1`, so deploying snapshot `COV-29`'s `after-return-500.wpj` to a **derived UUID** should show one detached fixture at `+39 % / +9 %`. | device-confirmed (the history and `projectChanged`); the firmware's refusal hypothesized | §8.1 |
| COV-36 | 2026-09-01 | COV-33's control window, and it returned two things. **The pinned heads are pinned on the wire**: the four group-A fixtures whose travel limits are absent read **0 on pan coarse, pan fine, tilt coarse and tilt fine**, `min == max` on all sixteen channels — COV-35 confirmed where it had only been read in percent off a screen. **And the position on the wire was not the pad that was tapped.** Identified by value rather than by trusting the panel: the one movable head's 16-bit tilt is **6530**, and pad `Center` predicts 6530 against pad 1's 6948 — so the capture is of `Center`, which cost nothing because the control does not depend on which pad is live. That reading is the tilt model landing on the **unit** at 16 bits, a 13th exact point after POS-01/02's twelve. **The unplanned result is on pan, and it closes a documented hole.** `wpj_position` scored 11/12 on pan and ignored the slot's own `PAN` entirely. The wire gives fraction **0.45995**; `PAN + etalement(FAN, k, n) − 0.5` with PAN 52 %, FAN 60 %, k 1, n 6 gives **0.45999** — under half a DMX unit at 16 bits. **The FAN ramp is centred on 0.5 and offset by the slot's PAN**, and the old formula is the special case PAN = 50 %, which is exactly where both frozen captures sit: re-scored under the new rule they are unchanged, 5/6 and 6/6, the one known miss included. Nothing had ever exercised a slot away from centre — the same shape as COV-14 and COV-26, a third time. The same reading also fixes `n`: at **6** it lands, at 5 it misses by 136 DMX units, so the ramp counts the group's fixtures **including the unpatched one**. Both are one point each and the docstring says so. | device-confirmed | §8.1 |
| COV-35 | 2026-09-01 | An **absent** travel limit in record 106 is **0**, not "unset". COV-33's DMX design read absent `106.f5`/`f6` as the full range and predicted a tilt byte of 84 on the control pad; a photograph of the `FIXTURE LIMITS` screen **refuted it before the measurement** — `MIN PAN 0 %, MAX PAN 0 %, MIN TILT 0 %, MAX TILT 0 %`. The identification did not rest on which pad was selected — the only fixtures in that file whose four limit fields are all absent are display ranks 2-5 of group A, and ranks 0 and 1 both carry a non-zero `MAX PAN` — and the operator then named it: the panel's **DMX 33**, which is the file's address **32** at base 0 (EXP-06), **display rank 2**. So the entry is on one fixture and not on a set of four. That rank is also the one the file's single record-151 entry points at under the display-order reading, so **the fixture carrying the only stored offset is a pinned one** — its `+39 % / +9 %` can never reach the wire. `min == max == 0` pins the head: **four of the group's six moving heads cannot move**, and they emit 0 on pan and tilt whatever position is recalled and whatever offset they carry. `wpj_position` already reads the fields this way — `borne16(0)` on both ends collapses the range — so no code changes; what changes is that the reading is now measured instead of incidental, and §7.5 says it. **The consequence for COV-33 is that the wire cannot answer it**: the file's stored entry is `fixture = 2`, a pinned rank under the display-order reading, so the offset that matters is invisible on DMX and the encoder turn goes back to being the measurement. The capture survives as a control. This is the published-prediction method paying for itself in the cheapest possible way — the sheet was wrong, it was wrong **in writing**, and a photograph cost nothing. | device-confirmed | §7.5 |
| COV-33 | 2026-09-01 | The discriminator COV-32 left open, **planned and published before the measurement**: which side of the `150` ↔ `151` disagreement is stale. Sheet: [`corpus/experiments/COV-33/prediction.md`](../corpus/experiments/COV-33/prediction.md). Three photographs were taken first and **do not settle it** — what they give is the control and a screen this repository had never seen. The control: group A's first slot reads **PAN 53.55 %, TILT 35.11 %, FAN | CROSS 50.06 %, FOCUS OFFSET 99.94 %**, COV-11's four values to two decimals, **unchanged by the canonicalisation save** — the panel holds what the file holds for record 150's content, so only the slice fields are in question. The screen: under a position slot the panel carries a **per-fixture page** with `PAN OFFSET`, `TILT OFFSET`, `FOCUS OFFSET` and a `FIXTURE | ON` encoder — the record-151 editor, photographed for the first time, reading 0 % on the fixture it happened to show, which is what the file predicts for that fixture. The measurement is **one encoder turn and no save**: the file stores one entry at `fixture = 2` carrying **PAN 39.00 %, TILT 9.00 %**, and group A's six fixtures are display ranks 0-5 with rank 0 unaddressed. Prediction: exactly one fixture shows 39/9, it is the one at DMX 32, and slots 4 and 5 show nothing — which would make the counters stale, the same failure as `120.f1` in the same save. The rival worth the sheet: four fixtures carry offsets, record 151 was **truncated on write**, and a device save can lose an operator's detached positions. **Sharpened before the measurement, from the vendor manual and the photograph's pixels:** the manual's `Fixture Offset Mode` page accounts for **one** marker, the selected fixture's offset as a white dot — which is the white centre of the green target, that fixture being at 0/0/0 — and the `Position Picker` page has none, as its photograph confirms. Isolating the UI green and clustering it finds **three** markers in the grid: the target and two round spots of ~24 px. Calibrated on the two axis lines against the group's device-confirmed `PAN 53.55 %` / `TILT 35.11 %` they sit near `(+42, +5)` and `(−58, −28)`; **the magnitudes are indicative** — the shot is angled and the second spot's absolute pan comes out below zero, which cannot be — but the **quadrants and the count** survive. One spot is `(+pan, +tilt)` with pan ≫ tilt, the signature of the file's only entry `+39 % / +9 %`; the other is deep in `(−pan, −tilt)`, and **the file holds no entry with a negative offset at all**. Calling the spots the other fixtures' offsets is an inference the manual does not make, which is why the encoder turn still has to happen. **Measured, and the rival at p 0.15 is what held.** The `FIXTURE` encoder turned through all six heads of group A on the operator-named pad: `Lyre Z 1` to `Lyre Z 6`, **`PAN OFFSET 0 %`, `TILT OFFSET 0 %`, `FOCUS OFFSET 0 %` on all six**, and the operator reports the same on **every** position pad. D2, D3 and D4 are refuted together; D5 held but for a larger reason than it stated; **D6 held**. So the panel carries **no detached fixture anywhere in this project**, while the file holds one record-151 entry at `PAN +39.00 %, TILT +9.00 %` and record 150 claims six slices of it. COV-32's question — *which side is stale, the counts or the content* — is therefore **mis-posed**: the device says neither, and the disagreement is not about a count but about whether the record has any content at all. **The inference this sheet built on is also refuted, and it was flagged as an inference**: the two small green markers on the grid are **not** the other fixtures' offsets — they are still there with every offset at zero, on every fixture and every pad. What they are is unattributed; a testable reading is that they are where the fixtures actually *point* after their travel limits, the four pinned heads (COV-35) stacked at pan 0 / tilt 0 in the corner and the one movable head elsewhere, which would explain exactly two markers that never move with the selection — and the DMX already places that head at 68.8 % / 10.0 % of full scale on pad `Center`, so one photograph of that pad would test it. **The experiment has a design fault and it is the author's:** the panel shows the **live copy** and the capture is the **stored file**, and the sheet never required a reload between them, so a divergence is exactly what it cannot attribute — the trap §10 and GEN-03 already record. The next step is one panel action: reload the project, then look again. | observed; the offsets device-confirmed absent, their cause not attributed | §8.1 |
| COV-32 | 2026-09-01 | A pre-change capture of the experiment project, taken after a save the operator **cannot attribute** — "nothing in particular". Downloaded **twice, byte-identical**, and the container's SHA-1 is checked against the body on the way in, so it is the device's state and not the link. Version counter +1: exactly one save since COV-29. Eight records are byte-for-byte identical, **165 included**; five moved. **Two identities fall, both on a file written after them.** (1) `120.f1` = 221 against **215** entries — the six last entries were deleted. Arithmetic decides which side is stale: 215 is exactly the sum of `channel_count` over each fixture's profile, so the **content** was corrected and the **counter** left behind. The surplus of six is not new — `len(120) − Σ channel_count = 6` on exactly **8 of 91** files, and they are exactly the eight that carry the misplaced `116.f12` (COV-30): one WTOOLS 2.0.2 session of COV-23, two artefacts, and this save repaired one of them. `comptes_de_tete` now checks containment on 120 alone. (2) `tranches_151`: group A's slots claim **6** entries of 151 where the record holds **1**, the slices still contiguous from 0 — and here **nothing decides which side is stale**, so the identity keeps only `referenced >= present` (nothing orphaned) and the discriminator, one `ALL POSITIONS` photograph, is not taken. Also written by the same save: all **20** rows of 115 gained `f6 = 231` and `f7 = 20`, absent before — and those two fields are **uniform within a file** on all 16 corpus files that carry them, one value per file, `f7` sometimes absent where `f6` is present. The lesson is COV-13's, larger: **a save the operator did not mean as an edit canonicalises**, and it can leave a counter behind while doing it. | observed | §7, §3.2 |
| COV-31 | 2026-09-01 | The bytes no name explains, swept for the ones that never **vary**: **24 paths carry exactly one value across the 90 files**, 4.15 % of the corpus — `165.f13` a zero-length array six times per preset (1.9 % on its own), `120.channels.f4` and eight other flags at 1, `115.f5` at 65535, `165.f26` at `00`, and §9's four prefix constants, verified at last against the file rather than quoted from the section. They are now a **fourth coverage bucket**, `inert`, printed beside `read` and never added to it: naming one `constant_1` would be a statement about the corpus wearing a name, and leaving them in `partial` says they are undecoded, which is equally false. What the bucket buys is that the claim is **checked** — each entry declares its value, the value is verified on every file, and a second one raises `InertBroken` and stops the run. That is trap 1 armed instead of suffered, and the two near-misses are why: **`116.profiles.f6` is 1 on ninety occurrences and 3 on one**, `102.f11` is 0 and 100; both stay `partial`. Corpus figures: **92.8 % read, 4.2 % inert, 3.0 % partial, 0.0 % unknown**. Two codec changes came with it and neither is a reading: `125.f4` is now a **split**, six bytes `profile_mask` (COV-26) and one byte `f4b6` that keeps a neutral key and lands in `partial` — a field understood in pieces, counted in pieces; and record **161** got a schema that names `items` and nothing else, which empties the `unknown` bucket without touching Q8. The corpus sweep is a statement about this corpus, exactly as trap 1 says; the value declarations are what make it falsifiable by the next file. | correlated | §3 |
| COV-30 | 2026-09-01 | `116.f12`, a field of **UTF-8 strings** nobody had opened — `Pan`, `Tilt`, `Pan/Tilt Speed`, `Strobe`, `Color Effect`, `Reset`, which are channel labels of the `FIXTURE BUILDER` kind. It exists in **8 corpus files only**, all one save lineage, first written by the COV-23 session, and it carries **two arrays**. **The names are not the carrying profile's.** The array on the 10-channel par (`offset_110 = 0`, own channels *Dimmer, Red, Green, Blue, extra1, extra2, extra3, Shutter, Generic, Speed*) names Pan at index 2 and Tilt at index 3. Searching every offset of the 37-channel record 110 for one where all five non-empty entries fit the `110.f4` type gives **exactly one candidate out of 28: offset 21**, which is `116.f3` of the 16-channel moving head — COV-25's profile. The cross-check is on a device-confirmed reading: `Color Effect` and `Reset` are name for name the two labels that screen prints on its two `Generic` channels. The rule then closes **all five items with no exception**: item i's `f12[k]` names the moving head's *own* channel `offset_110(i) + k` — item 0 → its channels 0-9 (10 entries), item 1 → channel 10, item 2 → 11-12, item 3 → 13-15 (`'', '', 'Reset'`), item 4 → past the end of a 16-entry array, and the two items whose slice holds no named channel emit no field at all. So **WTOOLS 2.0.2 cut one profile-local array with each item's global offset into record 110** — COV-25's own trap, committed by the vendor's editor in the other direction, which is why the profile the names belong to carries none of them. **Nothing is named**: the one established fact is that the array is *not* the channel names of the profile that holds it, and `channel_names` on that item would be the exact lie this codec avoids. A reader that trusts `f12` gets another profile's labels. Eight derived files, one project, one session — the corpus cannot take this past `observed`; opening `FIXTURE BUILDER` on the moving head and saving would. | observed | §7.6 |
| COV-05 | 2026-09-01 | `105.f1` against `115[f5].f3`, both named "profile" by the codec → **they never agree**, on any of the 1394 patch entries. `105.f1` is the library fixture id, `115.f3` an index into record 116: one name covered two key spaces. Renamed `library_id`. | correlated | §7 |

### The FX sub-message

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| FX2-01 | 2026-08-27 | "Double Trouble": firmware 2.0 doubles the FX engines, and the registry is asked whether it can see it → an engine that is on stores two page configurations, `correlated 45/45` — **refuted the same evening** by one preset composed at the panel. | validated (one slice per engine); the prediction and one identity refuted | §5.6 |
| FX6-01 | 2026-08-30 | The three FX screens of the manual read side by side → the registry's "four properties for three fields" count was **wrong**. What it retracts is a count, not a status. | observed | §5 |
| FX6-02 | 2026-08-30 | `Phase` / `Size` / `Fade` set to mutually distinct values absent from the factory vocabulary, `LIVE EDIT` off → the three-way permutation was a **four-way**: `f2` is the fade and the speed is `f9`, after ACC-04 had left `f2` named "speed %" and unexamined for four days. | validated | §5 |
| FX6-03 | 2026-08-30 | The Beam FX `Feature`, three independent sources pointing at one slot and none having measured it → measured. | validated | §5 |
| FX6-04 | 2026-08-30 | `f3` on the movement engine → the **`Fan`**, one byte out of 46414. | validated | §5 |
| FX6-05 | 2026-08-30 | The beam sequencer, hunted in the opaque record 161 → it is in record **155**, behind `f2`, a field that already carried the name "sequence flavour" and had never been opened. 155 was `device-confirmed` with a reading that was too general. | validated | §8 |

### The show generator, and the guards

| id | date | manipulation → result | status | SPEC § |
|---|---|---|---|---|
| GEN-01 | 2026-08-27 | Intention → `.wpj` → deploy → recall, with the rig read from the donor rather than described → `f31` written comes out exactly as it reads; the **dimmers do not**. | device-confirmed | §10 |
| GEN-02 | 2026-08-27 | The operator settles the three open readings, and none wins: the faders are all at 100 % → the missing dimmer was a **controller setting**, off the whole time. `f17` is a percentage and reaches DMX through `106.f5`/`f6`. Downgrades the `OTHER` attribution — see the refutations. | device-confirmed | §10 |
| GEN-03 | 2026-08-27 | Five cues identical but for `f31`, to discriminate the record-102 anomaly → the anomaly is that **the live copy is not the file**: with `LIVE EDIT` on, one gesture at the panel rewrites a cue's live copy while the file stays as uploaded. Refutes "`f16` slice 5 is always 255" (`correlated` over 2446 presets), and the fixture index is sequential with 0 omitted. | device-confirmed | §5.4, §10 |
| SPLIT-01 | 2026-08-31 | Six measures re-running read, named mode, preset recall and one experiment deploy after phase 2 split the device path into four modules → **six conforming**. Measure 4 confirms SCREEN-02's inertia from a *different* starting screen, which is the prediction its author had got wrong. Measure 6's wording was looser than the tool: the read-back differs by SHA-256 and is identical **record by record**, only the version counter at bytes 40–43 moving — which is what `verify_project` compares, and what SPEC §9 measured. | device-confirmed | §9, §10.2 |
| GUARD-01 | 2026-08-31 | The phase-1 refusals, on the device: nine measures checking that each guard refuses **before** anything reaches the link and that none breaks a path that worked → eight conforming, one refuted and fixed in the session. | device-confirmed | — |
